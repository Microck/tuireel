---
phase: 23-docs-theme-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/docs.json, scripts/verify-brand-system.ts]
autonomous: false

must_haves:
  truths:
    - "The docs header/navigation displays the v1.15 logo (light and dark variants)"
    - "The docs UI uses the v1.15 palette for primary/accent/background styling"
    - "The docs favicon matches the v1.15 favicon asset in the browser tab"
    - "The Mintlify theme field is set, eliminating schema validation warnings"
  artifacts:
    - path: "docs/docs.json"
      provides: "Mintlify docs configuration with theme + brand tokens"
      contains: '"theme": "mint"'
    - path: "scripts/verify-brand-system.ts"
      provides: "Drift check asserting theme field exists"
      contains: "theme"
  key_links:
    - from: "docs/docs.json"
      to: "assets/branding/palette.json"
      via: "verify-brand-system.ts drift checks"
      pattern: "theme"
---

<objective>
Add the required Mintlify `theme` field to docs.json, harden the drift-check script to assert it exists, and visually verify the rendered docs match the v1.15 brand system.

Purpose: Phase 20 wired all brand tokens (colors, logo, favicon) into docs.json, but omitted the `theme` field which Mintlify marks as required. This plan closes that gap and performs the visual verification the success criteria demand.

Output: docs.json with `theme` set, updated drift check, visual confirmation that the docs render correctly with the v1.15 palette in both light and dark modes.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@docs/docs.json
@scripts/verify-brand-system.ts
@assets/branding/palette.json
@.planning/phases/23-docs-theme-alignment/23-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add theme field to docs.json and harden drift check</name>
  <files>docs/docs.json, scripts/verify-brand-system.ts</files>
  <action>
1. In `docs/docs.json`, add `"theme": "mint"` as a top-level field right after the `"$schema"` line (before `"name"`). This is the required Mintlify theme field — `mint` is the safe default (classic layout, most battle-tested, matches the site's current implicit behavior).

2. In `scripts/verify-brand-system.ts`:
   - In the `DocsJson` type (around line 116), add `theme?: string` to the type definition.
   - In the `if (docsJson)` block that checks logo/favicon (around line 152), add a new assertion:
     ```typescript
     assertTruthy("docs/docs.json.theme", (docsJson as Record<string, unknown>).theme);
     ```
     This follows the existing pattern (uses `assertTruthy` to assert the field is present and non-empty, not `assertEqual` to a specific value — the exact theme choice is discretionary).

3. Run the drift check to confirm it passes with the new field:
   ```bash
   npx tsx scripts/verify-brand-system.ts
   ```
     </action>
     <verify>
       Run `npx tsx scripts/verify-brand-system.ts` — must print "Brand system verification passed." with exit code 0.
       Run `grep '"theme"' docs/docs.json` — must show `"theme": "mint"`.
       Run `grep 'theme' scripts/verify-brand-system.ts` — must show the new assertion.
     </verify>
     <done>
       docs/docs.json contains `"theme": "mint"` and the drift-check script asserts the theme field exists, both passing cleanly.
     </done>
   </task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All brand tokens (colors, logo, favicon, background, theme) are now wired in docs.json and protected by automated drift checks. The docs should render with the v1.15 palette in both light and dark modes.
  </what-built>
  <how-to-verify>
    1. Start the Mintlify dev server: `pnpm -C docs dev` (or `cd docs && npx mintlify dev`)
    2. Open the local URL in a browser (typically http://localhost:3000)
    3. Check LIGHT mode:
       - Header/nav shows the v1.15 logo (dark variant on light background)
       - Background is warm cream (#F5ECD9)
       - Primary accent color is teal (#18AAA7) on links/buttons
       - Favicon in browser tab matches the v1.15 favicon
    4. Toggle to DARK mode:
       - Header/nav shows the light logo variant
       - Background is deep dark (#141015)
       - Accent/hover color uses the brand red (#C1263B)
       - Favicon still correct
    5. Confirm the overall visual feel is consistent with the README's brand presentation
    6. Navigate to at least one content page to verify styling carries through
  </how-to-verify>
  <resume-signal>Type "approved" if the docs look correct in both modes, or describe any visual issues</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsx scripts/verify-brand-system.ts` passes (automated drift check including theme field)
2. `grep '"theme"' docs/docs.json` returns the mint theme line
3. Visual inspection confirms docs render correctly with v1.15 brand in light + dark modes
</verification>

<success_criteria>

- docs.json has all required Mintlify fields including `theme`
- verify-brand-system.ts asserts theme field presence
- Drift check passes with exit code 0
- Human has visually confirmed docs render with correct logo, colors, favicon in both light and dark modes
  </success_criteria>

<output>
After completion, create `.planning/phases/23-docs-theme-alignment/23-01-SUMMARY.md`
</output>
