---
phase: 06-workflow-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/preview.ts
  - packages/cli/src/commands/preview.ts
autonomous: true

must_haves:
  truths:
    - "tuireel preview executes all steps in a visible terminal"
    - "preview mode produces no video output files"
    - "preview logs each step as it executes (step type + summary)"
    - "preview exits cleanly after all steps complete"
  artifacts:
    - path: "packages/core/src/preview.ts"
      provides: "Preview engine: runs steps without capture/encoding"
      exports: ["preview"]
    - path: "packages/cli/src/commands/preview.ts"
      provides: "CLI preview command wiring"
  key_links:
    - from: "packages/core/src/preview.ts"
      to: "packages/core/src/executor/step-executor.ts"
      via: "Reuses executeSteps with onStepStart callback for logging"
      pattern: "executeSteps"
    - from: "packages/cli/src/commands/preview.ts"
      to: "packages/core/src/preview.ts"
      via: "imports preview() function"
      pattern: "import.*preview"
---

<objective>
Implement `tuireel preview` — runs the step script in a visible terminal without frame capture or video encoding.

Purpose: Lets users test their config scripts quickly without waiting for recording/encoding.
Output: preview() core function + preview CLI command.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-workflow-polish/06-RESEARCH.md
@packages/core/src/executor/step-executor.ts
@packages/core/src/session.ts
@packages/cli/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preview engine and CLI command</name>
  <files>
    packages/core/src/preview.ts
    packages/cli/src/commands/preview.ts
    packages/cli/src/index.ts
  </files>
  <action>
  Create `packages/core/src/preview.ts`:
  - Export `async function preview(config: Config): Promise<void>`
  - Create tuistory session with `{ visible: true }` option if supported, otherwise standard session
  - Use executeSteps with onStepStart callback that logs: `console.log(`▶ ${step.type}: ${summarizeStep(step)}`)`
  - Create a `summarizeStep(step)` helper: type→first 30 chars of text, press→key name, wait→pattern, pause→duration, etc.
  - Skip ALL frame capture and encoding — just run steps and log
  - Close session on completion. Handle errors with clear messages.

  Create `packages/cli/src/commands/preview.ts`:
  - Commander command: `tuireel preview [config]`
  - Default config path: `.tuireel.jsonc`
  - Load and validate config using existing config loader
  - Call `preview(config)` from core
  - Print "Preview complete" on success

  Wire into CLI: add preview command to the main commander program in index.ts.
  Export preview() from core barrel (index.ts).
  </action>
  <verify>tsc --noEmit passes. `tuireel preview --help` prints usage info.</verify>
  <done>preview command loads config, runs steps with console logging, produces no video output.</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` passes for both packages
- `tuireel preview --help` shows correct usage
- preview() function is exported from @tuireel/core
</verification>

<success_criteria>
- `tuireel preview` runs step script with step-by-step console output
- No video files are produced during preview
- Session closes cleanly after completion
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflow-polish/06-02-SUMMARY.md`
</output>
