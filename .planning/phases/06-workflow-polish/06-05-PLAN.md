---
phase: 06-workflow-polish
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03", "06-04"]
files_modified:
  - packages/cli/package.json
  - packages/cli/src/index.ts
  - packages/core/package.json
  - package.json
autonomous: false

must_haves:
  truths:
    - "npx tuireel --help prints CLI help"
    - "bunx tuireel --help prints CLI help"
    - "tuireel binary has shebang and correct bin field"
    - "All CLI commands are accessible (init, record, preview, validate, composite)"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "Publishable CLI package with bin, type:module, correct exports"
    - path: "packages/core/package.json"
      provides: "Publishable core package with correct exports and main fields"
  key_links:
    - from: "packages/cli/package.json"
      to: "dist/index.js"
      via: "bin.tuireel points to built entrypoint"
      pattern: '"bin".*"tuireel".*"dist/index.js"'
    - from: "packages/cli/src/index.ts"
      to: "shebang"
      via: "tsup banner adds #!/usr/bin/env node"
      pattern: "banner|shebang"
---

<objective>
Prepare packages for npm/Bun distribution so `npx tuireel` and `bunx tuireel` work.

Purpose: Makes Tuireel installable and runnable by end users.
Output: Correctly configured package.json files, verified npx/bunx compatibility.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-workflow-polish/06-RESEARCH.md
@packages/cli/package.json
@packages/core/package.json
@packages/cli/tsup.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure packages for npm distribution</name>
  <files>
    packages/cli/package.json
    packages/core/package.json
    packages/cli/tsup.config.ts
    package.json
  </files>
  <action>
  Update `packages/cli/package.json`:
  - name: "tuireel"
  - version: "0.1.0"
  - type: "module"
  - bin: { "tuireel": "./dist/index.js" }
  - main: "./dist/index.js"
  - files: ["dist"]
  - engines: { "node": ">=18" }
  - Ensure all runtime deps are in dependencies (not devDependencies): commander, @tuireel/core
  - Add description, keywords, license, repository fields

  Update `packages/core/package.json`:
  - name: "@tuireel/core"
  - version: "0.1.0"
  - type: "module"
  - main, types, exports fields pointing to dist/
  - files: ["dist"]
  - Ensure all runtime deps are in dependencies

  Verify tsup.config.ts for CLI package:
  - Must have `banner: { js: '#!/usr/bin/env node' }` for shebang
  - format: ["esm"]
  - clean: true

  Build both packages: `pnpm build`
  Verify dist/index.js starts with shebang line.
  </action>
  <verify>pnpm build succeeds. `head -1 packages/cli/dist/index.js` shows shebang. All package.json fields present.</verify>
  <done>Both packages configured for npm publishing with correct bin, type, exports.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>npm/Bun distribution configuration — packages ready for publishing</what-built>
  <how-to-verify>
    1. Run `pnpm build` — should succeed
    2. Run `node packages/cli/dist/index.js --help` — should print full CLI help with all commands
    3. Test npx locally: `cd /tmp && npx /path/to/packages/cli --help`
    4. Verify all commands listed: init, record, preview, validate, composite
    5. Check `head -1 packages/cli/dist/index.js` starts with `#!/usr/bin/env node`
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `pnpm build` succeeds for both packages
- CLI binary has shebang and prints help
- All Phase 6 commands (preview, record --watch) appear in help
- package.json has correct bin, type, files, exports fields
</verification>

<success_criteria>
- Both packages are publishable (correct package.json metadata)
- `node packages/cli/dist/index.js --help` shows all commands
- Shebang present in built CLI entrypoint
- Local npx test works
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflow-polish/06-05-SUMMARY.md`
</output>
