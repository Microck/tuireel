---
phase: 06-workflow-polish
plan: 06
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03", "06-04", "06-05"]
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/session.ts
  - packages/core/src/executor/steps/set-env.ts
  - packages/core/src/executor/step-executor.ts
  - packages/core/test/set-env.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "set-env step has user-visible runtime effect for subsequent scripted commands in the same session"
    - "set-env step keeps session environment state in sync for future launch compatibility"
    - "Invalid environment variable keys are rejected at config validation time"
    - "Automated test proves a command after set-env can observe the updated variable (STEP-09)"
  artifacts:
    - path: "packages/core/src/config/schema.ts"
      provides: "set-env key contract tightened to valid environment variable identifiers"
      exports: ["stepSchema", "TuireelStep"]
    - path: "packages/core/src/session.ts"
      provides: "Session helper to apply environment updates to the live terminal context"
      exports: ["TuireelSession"]
    - path: "packages/core/src/executor/steps/set-env.ts"
      provides: "set-env runtime application + in-memory env tracking"
    - path: "packages/core/src/executor/step-executor.ts"
      provides: "Idle-wait semantics aligned with set-env side effects"
      exports: ["executeSteps"]
    - path: "packages/core/test/set-env.test.ts"
      provides: "No-mock integration coverage for STEP-09 runtime observability"
  key_links:
    - from: "packages/core/src/config/schema.ts"
      to: "packages/core/src/config/parser.ts"
      via: "configSchema.safeParse enforces set-env key contract"
      pattern: "configSchema|safeParse|set-env"
    - from: "packages/core/src/executor/steps/set-env.ts"
      to: "packages/core/src/session.ts"
      via: "set-env updates env map and applies shell export command"
      pattern: "setEnv|writeRaw|export"
    - from: "packages/core/test/set-env.test.ts"
      to: "packages/core/src/executor/step-executor.ts"
      via: "executeSteps run validates runtime variable visibility"
      pattern: "executeSteps|set-env"
---

<objective>
Close the remaining Phase 6 blocker (STEP-09) by making `set-env` observable at runtime and protected by tests.

Purpose: eliminate write-only env state so users can rely on mid-script environment updates in real recordings/previews.
Output: validated env-key contract, runtime env application path, and regression coverage proving post-set commands see updated variables.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-workflow-polish/06-workflow-polish-VERIFICATION.md
@.planning/phases/06-workflow-polish/06-01-SUMMARY.md
@packages/core/src/config/schema.ts
@packages/core/src/session.ts
@packages/core/src/executor/steps/set-env.ts
@packages/core/src/executor/step-executor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten set-env config contract for runtime-safe keys</name>
  <files>packages/core/src/config/schema.ts</files>
  <action>
    Update set-env schema to validate env keys as identifiers suitable for shell export and process env usage:
    - Keep backward compatibility for values (`value` remains string).
    - Constrain `key` to environment-variable identifier format (for example: `^[A-Za-z_][A-Za-z0-9_]*$`).
    - Emit actionable validation message when key format is invalid.

    Guardrails:
    - Do not change step names or top-level config shape.
    - Keep existing wait regex and include schema behavior untouched.
  </action>
  <verify>
    `pnpm --filter @tuireel/core exec tsc --noEmit`
  </verify>
  <done>
    Invalid set-env keys fail fast during config validation instead of causing shell/runtime ambiguity later.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make set-env mutate live session behavior, not only local state</name>
  <files>
    packages/core/src/session.ts
    packages/core/src/executor/steps/set-env.ts
    packages/core/src/executor/step-executor.ts
  </files>
  <action>
    Implement a concrete runtime application path for set-env:
    - Keep `session.env[key] = value` state updates.
    - Add a `TuireelSession` method that applies an env update in the active terminal context (for example via shell-safe `export KEY='value'` command emission).
    - Update `set-env` step handler to call that method.
    - Revisit idle wait behavior so set-env step completion waits for terminal settle if it now emits a command.

    Constraints:
    - Preserve existing behavior for all non-set-env step types.
    - Ensure quoting/escaping is deterministic and safe for values containing spaces or quotes.
  </action>
  <verify>
    `pnpm --filter @tuireel/core exec tsc --noEmit`
  </verify>
  <done>
    set-env has observable effect during script execution and no longer remains write-only state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add no-mock integration coverage for STEP-09 runtime observability</name>
  <files>packages/core/test/set-env.test.ts</files>
  <action>
    Add a focused integration test that exercises real execution flow:
    - Launch an interactive shell session.
    - Execute steps including `set-env` then a command that reads that variable.
    - Assert the variable value appears via wait/assertion path after set-env.
    - Add a negative/guard assertion for invalid env key validation behavior.

    Keep test practical and deterministic:
    - Prefer direct core APIs (`createSession` + `executeSteps`) with real session behavior.
    - Avoid module mocks per project testing policy.
  </action>
  <verify>
    `pnpm --filter @tuireel/core test -- test/set-env.test.ts`
    `pnpm --filter @tuireel/core test`
  </verify>
  <done>
    STEP-09 is enforced by automated runtime evidence, not only schema presence.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @tuireel/core test -- test/set-env.test.ts
pnpm --filter @tuireel/core test
pnpm --filter @tuireel/core exec tsc --noEmit
pnpm build
```
</verification>

<success_criteria>
- `set-env` changes are observable by subsequent scripted commands in the same run
- set-env key contract blocks invalid identifiers before runtime
- No-mock integration test guards STEP-09 against regression
- Phase 6 verification gap closes with zero new regressions
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflow-polish/06-06-SUMMARY.md`
</output>
