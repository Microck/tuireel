---
phase: 06-workflow-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/executor/steps/scroll.ts
  - packages/core/src/executor/steps/click.ts
  - packages/core/src/executor/steps/screenshot.ts
  - packages/core/src/executor/steps/resize.ts
  - packages/core/src/executor/steps/set-env.ts
  - packages/core/src/executor/step-executor.ts
  - packages/core/src/config/schema.ts
autonomous: true

must_haves:
  truths:
    - "scroll step sends mouse scroll events to the terminal session"
    - "click step finds text pattern and clicks on it"
    - "screenshot step captures a PNG to the specified output path"
    - "resize step changes terminal cols/rows mid-recording"
    - "set-env step updates environment variables for subsequent steps"
  artifacts:
    - path: "packages/core/src/executor/steps/scroll.ts"
      provides: "Scroll step handler using tuistory mouse scroll API"
    - path: "packages/core/src/executor/steps/click.ts"
      provides: "Click step handler using tuistory clickText API"
    - path: "packages/core/src/executor/steps/screenshot.ts"
      provides: "Screenshot step capturing PNG to file"
    - path: "packages/core/src/executor/steps/resize.ts"
      provides: "Resize step calling session.resize()"
    - path: "packages/core/src/executor/steps/set-env.ts"
      provides: "Set-env step updating session environment"
  key_links:
    - from: "packages/core/src/executor/step-executor.ts"
      to: "packages/core/src/executor/steps/*.ts"
      via: "switch cases dispatching to new step handlers"
      pattern: 'case "scroll"|case "click"|case "screenshot"|case "resize"|case "set-env"'
---

<objective>
Add 5 advanced step types to the step executor: scroll, click, screenshot, resize, set-env.

Purpose: Completes the step type vocabulary for realistic TUI demo recordings.
Output: 5 new step handler files, updated executor dispatch, updated config schema.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-workflow-polish/06-RESEARCH.md
@packages/core/src/executor/step-executor.ts
@packages/core/src/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 5 advanced step type schemas and handlers</name>
  <files>
    packages/core/src/config/schema.ts
    packages/core/src/executor/steps/scroll.ts
    packages/core/src/executor/steps/click.ts
    packages/core/src/executor/steps/screenshot.ts
    packages/core/src/executor/steps/resize.ts
    packages/core/src/executor/steps/set-env.ts
  </files>
  <action>
  Add Zod schemas for 5 new step types to the existing step union in config/schema.ts:
  - `scroll`: { type: "scroll", direction: "up"|"down", amount?: number (default 3 lines) }
  - `click`: { type: "click", pattern: string } — text to find and click
  - `screenshot`: { type: "screenshot", output: string } — PNG output path
  - `resize`: { type: "resize", cols: number, rows: number }
  - `set-env`: { type: "set-env", key: string, value: string }

  Create step handler files following existing pattern (launch/type/press/wait/pause):
  - scroll.ts: Use session.scroll(direction, amount) or equivalent tuistory mouse scroll API
  - click.ts: Use session.clickText(pattern) — find text in terminal and click coordinates
  - screenshot.ts: Call session.screenshot() and write PNG buffer to step.output path using fs.writeFile
  - resize.ts: Call session.resize(step.cols, step.rows)
  - set-env.ts: Set session.env[step.key] = step.value (updates for subsequent commands)
  </action>
  <verify>tsc --noEmit passes. New schemas are part of the step union type.</verify>
  <done>All 5 step type schemas defined and handlers implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire new steps into executor dispatch</name>
  <files>packages/core/src/executor/step-executor.ts</files>
  <action>
  Add 5 new cases to the step executor switch/dispatch:
  - "scroll" → scrollStep(session, step)
  - "click" → clickStep(session, step)
  - "screenshot" → screenshotStep(session, step)
  - "resize" → resizeStep(session, step)
  - "set-env" → setEnvStep(session, step)

  Import all handlers. Ensure session.waitIdle() is called after scroll, click, and resize (not after screenshot or set-env as those don't affect terminal state in a way that needs settling).
  Re-export new step types from the core barrel export (index.ts) if needed.
  </action>
  <verify>tsc --noEmit passes. All 10 step types (5 original + 5 new) are handled — no unhandled case in switch.</verify>
  <done>Step executor dispatches all 10 step types. No exhaustive switch gaps.</done>
</task>

</tasks>

<verification>
- `tsc --noEmit` passes across the core package
- Config schema includes all 5 new step types in the union
- Step executor has cases for scroll, click, screenshot, resize, set-env
- JSON Schema generation includes the new step types
</verification>

<success_criteria>
- All 5 new step types have Zod schemas and handler implementations
- Executor dispatch covers all step types with no gaps
- Screenshot writes PNG to disk, not just returns buffer
</success_criteria>

<output>
After completion, create `.planning/phases/06-workflow-polish/06-01-SUMMARY.md`
</output>
