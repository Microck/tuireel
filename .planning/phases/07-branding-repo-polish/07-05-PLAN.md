---
phase: 07-branding-repo-polish
plan: 05
type: execute
wave: 3
depends_on: ["07-01", "07-02", "07-03", "07-04"]
files_modified:
  - README.md
autonomous: false

must_haves:
  truths:
    - "README shows branded logo with dark/light mode switching"
    - "README has badges for npm version, CI status, and license"
    - "README has install command, quickstart example, and docs link"
    - "README hero section shows the banner image"
  artifacts:
    - path: "README.md"
      provides: "Branded project README with all required sections"
      contains: "tuireel"
      min_lines: 50
  key_links:
    - from: "README.md"
      to: "assets/branding/logo-dark.svg"
      via: "picture element source for dark mode"
      pattern: "logo-dark\\.svg"
    - from: "README.md"
      to: "assets/branding/logo-light.svg"
      via: "picture element source for light mode"
      pattern: "logo-light\\.svg"
    - from: "README.md"
      to: "assets/branding/banner.png"
      via: "img element for hero banner"
      pattern: "banner\\.png"
    - from: "README.md"
      to: "LICENSE"
      via: "License section footer links to LICENSE file"
      pattern: "LICENSE"
    - from: "README.md"
      to: "CONTRIBUTING.md"
      via: "Contributing section links to CONTRIBUTING.md"
      pattern: "CONTRIBUTING"
---

<objective>
Create the branded README.md that ties together all branding assets, badges, and community links.

Purpose: The README is the project's front door. It must show the logo (with dark/light switching), banner, badges, install command, quickstart, and links to docs/contributing/license. This is the final assembly step — it references assets from all prior plans.

Output: `README.md` at repo root.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-branding-repo-polish/07-RESEARCH.md
@.planning/phases/07-branding-repo-polish/07-01-SUMMARY.md
@.planning/phases/07-branding-repo-polish/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create branded README.md</name>
  <files>README.md</files>
  <action>
Create `README.md` at repo root following the standard CLI tool README section order from research.

**Section order and content:**

1. **Logo** (centered) — Use `<picture>` element for dark/light mode switching:
```html
<p align="center">
  <picture>
    <source media="(prefers-color-scheme: dark)" srcset="assets/branding/logo-dark.svg">
    <source media="(prefers-color-scheme: light)" srcset="assets/branding/logo-light.svg">
    <img alt="Tuireel" src="assets/branding/logo-light.svg" width="360">
  </picture>
</p>
```

2. **Banner** — Centered banner image below the logo:
```html
<p align="center">
  <img src="assets/branding/banner.png" alt="Tuireel — Scripted TUI demo recorder" width="640">
</p>
```

3. **One-line description** (centered): "Scripted TUI demo recorder — produce polished terminal demo videos from declarative configs."

4. **Badges row** (centered) — exactly 3 badges:
   - npm version: `[![npm version](https://img.shields.io/npm/v/tuireel)](https://www.npmjs.com/package/tuireel)`
   - CI: `[![CI](https://github.com/tuireel/tuireel/actions/workflows/ci.yml/badge.svg)](https://github.com/tuireel/tuireel/actions/workflows/ci.yml)` (placeholder until Phase 11 — shields.io shows "no status" gracefully)
   - License: `[![license](https://img.shields.io/npm/l/tuireel)](./LICENSE)`

5. **Install** — code block with `npm install -g tuireel` (also mention `npx tuireel` and `bunx tuireel` as alternatives)

6. **Quick Start** — minimal example showing:
   - Run `tuireel init` to scaffold a config
   - Run `tuireel record` to produce a video
   - Brief mention of what the config looks like (3-4 lines of JSONC)

7. **Features** — 5-6 bullet points:
   - Declarative JSONC config with JSON Schema support
   - MP4, WebM, GIF output formats
   - Optional cursor overlay, keystroke HUD, sound effects
   - Terminal themes (built-in + custom)
   - Watch mode for iterative development
   - Multi-video configs with shared step includes

8. **Documentation** — Link to docs (placeholder: `https://tuireel.dev` or `docs/` — Phase 10 will create the actual site)

9. **Contributing** — Short paragraph + link to `CONTRIBUTING.md`

10. **License** — "Apache 2.0 — see [LICENSE](./LICENSE)"

**Constraints:**
- Keep under 150 lines total (link to docs for details)
- No badge overload (exactly 3)
- Use the `tuireel` package name for badges (CLI package, not `@tuireel/core`)
- All image paths should be relative (`assets/branding/...`)
  </action>
  <verify>
```bash
# File exists and has reasonable length
wc -l README.md

# Contains required sections
grep -c 'picture' README.md    # dark/light logo switching
grep -c 'banner.png' README.md  # banner image
grep -c 'shields.io' README.md  # badges
grep -c 'npm install' README.md # install command
grep -c 'tuireel init' README.md # quickstart
grep -c 'CONTRIBUTING' README.md # contributing link
grep -c 'LICENSE' README.md     # license link
```
All should return >= 1.
  </verify>
  <done>README.md exists under 150 lines with logo (dark/light picture element), banner, 3 badges (npm, CI, license), install instructions, quickstart, features list, docs link, contributing link, and license footer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete branding and repo polish</name>
  <what-built>
Complete Phase 7 output: branded README with logo/banner, Apache 2.0 LICENSE, CONTRIBUTING.md, GitHub issue/PR templates, all branding assets (SVGs + PNGs).
  </what-built>
  <how-to-verify>
1. Open the repository on GitHub (or view locally) and check:
   - README renders with logo, banner image, badges, and all sections
   - Logo switches between dark/light variants based on your GitHub theme preference (toggle theme in GitHub settings to verify)

2. Verify branding assets exist:
   ```bash
   ls -la assets/branding/
   ```
   Should show: logo.svg, logo-dark.svg, logo-light.svg, favicon.svg, banner.png, og-image.png

3. Verify LICENSE is detected:
   ```bash
   head -3 LICENSE
   grep '"license"' packages/cli/package.json packages/core/package.json
   ```

4. Verify GitHub templates:
   ```bash
   find .github -type f | sort
   ```

5. Verify CONTRIBUTING.md references correct tools:
   ```bash
   grep -E 'pnpm|turbo|vitest' CONTRIBUTING.md
   ```

6. Check overall visual quality — does the logo look professional? Is the color palette cohesive? Is the README clean and scannable?
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
Full phase verification — all 10 requirements:

```bash
echo "=== BRND-01: Logo SVG at favicon size, dark/light ==="
ls assets/branding/logo.svg assets/branding/logo-dark.svg assets/branding/logo-light.svg

echo "=== BRND-02: Color palette (check SVGs for #06B6D4) ==="
grep -l '06B6D4' assets/branding/*.svg

echo "=== BRND-03: Banner 1280x640 ==="
ls -la assets/branding/banner.png

echo "=== BRND-04: OG image 1200x630 ==="
ls -la assets/branding/og-image.png

echo "=== BRND-05: Favicon ==="
ls assets/branding/favicon.svg

echo "=== REPO-01: README with logo, badges, install, quickstart ==="
wc -l README.md
grep -c 'shields.io' README.md

echo "=== REPO-02: LICENSE + package.json ==="
head -1 LICENSE
grep '"license"' packages/cli/package.json packages/core/package.json

echo "=== REPO-03: CONTRIBUTING.md ==="
wc -l CONTRIBUTING.md

echo "=== REPO-04: Issue templates ==="
ls .github/ISSUE_TEMPLATE/

echo "=== REPO-05: PR template ==="
ls .github/pull_request_template.md
```
</verification>

<success_criteria>
- README.md is under 150 lines with all required sections
- Logo uses `<picture>` element for dark/light mode
- Banner image displayed in README hero
- 3 badges: npm version, CI, license (all using shields.io)
- Install, quickstart, features, docs, contributing, license sections present
- Human has verified visual quality and approved
- Covers: REPO-01 (README), and serves as integration checkpoint for all BRND-* and REPO-* requirements
</success_criteria>

<output>
After completion, create `.planning/phases/07-branding-repo-polish/07-05-SUMMARY.md`
</output>
