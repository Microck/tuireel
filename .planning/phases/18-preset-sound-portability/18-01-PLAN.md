---
phase: 18-preset-sound-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/sound.ts
autonomous: true

must_haves:
  truths:
    - "@tuireel/core tarball includes built-in SFX assets (assets/sounds/*.mp3)"
    - "Built-in SFX paths resolve package-relatively (not via process.cwd)"
    - "Sound path resolution works for both ESM and CJS builds"
  artifacts:
    - path: packages/core/package.json
      provides: "npm publish allowlist includes assets/"
      contains: '"files"'
    - path: packages/core/src/sound.ts
      provides: "Built-in SFX directory resolution"
      exports: [resolveSfxPath, ensureSoundAssets]
  key_links:
    - from: packages/core/package.json
      to: packages/core/assets/sounds
      via: "files allowlist"
      pattern: "\\\"files\\\"\\s*:\\s*\\["
    - from: packages/core/src/sound.ts
      to: packages/core/assets/sounds
      via: "import.meta.url / __dirname based resolution"
      pattern: "import\\.meta\\.url|__dirname"
---

<objective>
Fix built-in preset SFX portability by shipping @tuireel/core assets and resolving them relative to the installed package (not the caller's CWD).

Purpose: Presets like `polished` reliably enable sound when tuireel runs via npx/bunx from a non-repo directory.
Output: Published core package contains `assets/sounds/*.mp3` and runtime code resolves those assets deterministically.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/18-preset-sound-portability/18-RESEARCH.md

@packages/core/package.json
@packages/core/src/sound.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ship built-in SFX assets in @tuireel/core tarball</name>
  <files>packages/core/package.json</files>
  <action>
Update the publish allowlist so the core package ships `assets/` in addition to `dist/`.

Implementation details:

- Edit `packages/core/package.json` `files` array to include `assets` (keep `dist`).
- Do not broaden the allowlist beyond what is needed for built-in SFX portability.
  </action>
  <verify>
  pnpm --filter "@tuireel/core" build
  pnpm --filter "@tuireel/core" pack --pack-destination /tmp/tuireel-pack
  CORE_TGZ="$(ls -1 /tmp/tuireel-pack/*.tgz | rg -im 1 'core')"
  tar -tf "$CORE_TGZ" | rg "^package/assets/sounds/(click|key)-1\\.mp3$"
  </verify>
  <done>
  Packed `@tuireel/core` tarball contains `package/assets/sounds/click-1.mp3` and `package/assets/sounds/key-1.mp3`.
  </done>
  </task>

<task type="auto">
  <name>Task 2: Resolve built-in SFX directory package-relatively (no process.cwd dependency)</name>
  <files>packages/core/src/sound.ts</files>
  <action>
Replace the current CWD-first asset directory heuristics with a package-relative resolver.

Rules:

- Prefer module-relative resolution first (works for installed usage):
  - ESM path: derive from `import.meta.url` (via `fileURLToPath` + `dirname`).
  - CJS path: fall back to `__dirname` when available.
- From the current file location (src or dist), resolve `../assets/sounds`.
- Keep the existing CWD-based candidates only as last-resort fallbacks for local dev overrides.
- Keep current behavior for custom user-provided string paths (those remain as provided).

Note: `resolveSfxPath(undefined|number, ...)` must map to the shipped built-in mp3 files regardless of the caller's working directory.
</action>
<verify>
pnpm --filter "@tuireel/core" test
pnpm --filter "@tuireel/core" build
node --input-type=module -e "import { resolveSfxPath } from './packages/core/dist/index.js'; console.log(resolveSfxPath(undefined,'click'))"
node -e "const { resolveSfxPath } = require('./packages/core/dist/index.cjs'); console.log(resolveSfxPath(undefined,'key'))"
</verify>
<done>
In both ESM and CJS builds, default SFX paths resolve to a path under `packages/core/assets/sounds/` (not under `process.cwd()/assets`).
</done>
</task>

</tasks>

<verification>
- Core unit tests pass: `pnpm --filter "@tuireel/core" test`
- Packed tarball contains SFX assets (tar listing matches expected `package/assets/sounds/*.mp3`)
</verification>

<success_criteria>

- @tuireel/core published surface includes `assets/sounds/*.mp3`.
- Built-in SFX default resolution is package-relative and does not rely on `process.cwd()`.
  </success_criteria>

<output>
After completion, create `.planning/phases/18-preset-sound-portability/18-01-SUMMARY.md`
</output>
