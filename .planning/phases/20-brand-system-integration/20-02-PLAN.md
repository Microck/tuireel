---
phase: 20-brand-system-integration
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - scripts/verify-brand-system.ts
autonomous: true

must_haves:
  truths:
    - "assets/branding/palette.json is treated as the canonical palette source"
    - "Mintlify docs theme wiring (docs/docs.json) references the expected logo + favicon paths"
    - "Docs logo + favicon assets remain in sync between assets/branding and docs/images"
  artifacts:
    - path: "scripts/verify-brand-system.ts"
      provides: "Automated drift checks for palette + docs wiring"
      contains: "docs/docs.json"
  key_links:
    - from: "docs/docs.json"
      to: "docs/images/logo-light.svg"
      via: "logo.light"
      pattern: "\\/images\\/logo-light\\.svg"
    - from: "docs/docs.json"
      to: "docs/images/logo-dark.svg"
      via: "logo.dark"
      pattern: "\\/images\\/logo-dark\\.svg"
    - from: "docs/docs.json"
      to: "docs/images/favicon.svg"
      via: "favicon.(light|dark)"
      pattern: "\\/images\\/favicon\\.svg"
---

<objective>
Eliminate brand drift by adding a lightweight verification script that asserts the canonical palette + docs wiring + duplicated asset sync.

Purpose: Phase 20 is drift-prevention across brand consumers (README + Mintlify docs) while keeping the palette tokens authoritative.
Output: An automated check script that fails loudly when docs wiring or duplicated assets drift.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-brand-system-integration/20-RESEARCH.md

@assets/branding/palette.json
@docs/docs.json
@docs/images/logo-light.svg
@docs/images/logo-dark.svg
@docs/images/favicon.svg
@assets/branding/logo-light.svg
@assets/branding/logo-dark.svg
@assets/branding/favicon.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an automated brand wiring drift check script</name>
  <files>scripts/verify-brand-system.ts</files>
  <action>
Create `scripts/verify-brand-system.ts` (TypeScript) that exits non-zero if brand wiring drifts.

Checks to implement:

- `assets/branding/palette.json` contains exactly these keys: background, surface, primary, secondary, accent.

- `assets/branding/palette.json` matches the locked v1.15 token values:
  - background == "#141015"
  - surface == "#1C346D"
  - primary == "#18AAA7"
  - secondary == "#F5ECD9"
  - accent == "#C1263B"
- `docs/docs.json` values match palette mapping:
  - colors.primary == palette.primary
  - colors.light == palette.primary
  - colors.dark == palette.accent (keep current behavior)
  - background.color.light == palette.secondary
  - background.color.dark == palette.background

- `docs/docs.json` references the expected assets:
  - logo.light == "/images/logo-light.svg"
  - logo.dark == "/images/logo-dark.svg"
  - favicon.light == "/images/favicon.svg"
  - favicon.dark == "/images/favicon.svg"
- Docs images are byte-equal to branding sources:
  - docs/images/logo-light.svg == assets/branding/logo-light.svg
  - docs/images/logo-dark.svg == assets/branding/logo-dark.svg
  - docs/images/favicon.svg == assets/branding/favicon.svg

- README references the logo assets (basic BRND-06 guard):
  - README contains assets/branding/logo-light.svg
  - README contains assets/branding/logo-dark.svg

Implementation constraints:

- Use only Node built-ins (fs/path/url) and JSON parsing.
- Print a clear error per failed assertion and end with `process.exit(1)` if any fail.
- Print a single success line if all checks pass.

Then run it once:

- `pnpm -w exec tsx scripts/verify-brand-system.ts`
  </action>
  <verify>
  pnpm -w exec tsx scripts/verify-brand-system.ts
  </verify>
  <done>
  scripts/verify-brand-system.ts exists and passes locally; it fails loudly if palette/docs wiring or duplicated assets drift.
  </done>
  </task>

</tasks>

<verification>
- `pnpm -w exec tsx scripts/verify-brand-system.ts` passes
</verification>

<success_criteria>

- Drift checks exist and validate palette schema + docs.json wiring + duplicated docs assets
  </success_criteria>

<output>
After completion, create `.planning/phases/20-brand-system-integration/20-02-SUMMARY.md`
</output>
