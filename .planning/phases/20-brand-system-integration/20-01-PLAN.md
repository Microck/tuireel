---
phase: 20-brand-system-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - assets/branding/brand-system.md
  - assets/branding/palette.json
  - assets/branding/logo.svg
  - docs/docs.json
  - docs/images/logo-light.svg
  - docs/images/logo-dark.svg
  - docs/images/favicon.svg
  - README.md
  - .planning/STATE.md
autonomous: true

must_haves:
  truths:
    - "A user can identify assets/branding/palette.json as the single source-of-truth for the v1.15 palette"
    - "README references the v1.15 brand system source-of-truth without duplicating token hex values"
    - "Planning docs no longer claim the old palette as current"
  artifacts:
    - path: "assets/branding/palette.json"
      provides: "Canonical v1.15 palette tokens"
      contains: "background"
    - path: "assets/branding/brand-system.md"
      provides: "Documented source-of-truth location for palette tokens"
      contains: "assets/branding/palette.json"
    - path: "README.md"
      provides: "Public repo surface references v1.15 branding assets"
      contains: "assets/branding/logo-"
    - path: "docs/docs.json"
      provides: "Mintlify theme wiring for logo/favicon and colors"
      contains: '"logo"'
    - path: "docs/images/logo-light.svg"
      provides: "Docs-consumed light logo"
    - path: "docs/images/logo-dark.svg"
      provides: "Docs-consumed dark logo"
    - path: "docs/images/favicon.svg"
      provides: "Docs-consumed favicon"
  key_links:
    - from: "assets/branding/brand-system.md"
      to: "assets/branding/palette.json"
      via: "documented source-of-truth link"
      pattern: "assets/branding/palette\\.json"
    - from: "README.md"
      to: "assets/branding/brand-system.md"
      via: "brand system reference"
      pattern: "assets/branding/brand-system\\.md"
---

<objective>
Make the v1.15 palette tokens discoverable as a single in-repo source-of-truth, and remove stale references to the pre-v1.15 palette.

Purpose: Phase 20 requires a canonical palette + logo system that renders consistently and is easy to reference without drift.
Output: A small brand system doc pointing to the canonical palette file, plus README + STATE.md updates to reference it.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-brand-system-integration/20-RESEARCH.md

@assets/branding/palette.json
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the v1.15 brand system source-of-truth</name>
  <files>assets/branding/brand-system.md</files>
  <action>
Create a short, drift-resistant document that makes `assets/branding/palette.json` the explicit single source-of-truth for palette tokens.

Constraints:

- Do NOT duplicate the token hex values in this markdown (avoid drift). Refer to the JSON file for exact values.
- Document which repo surfaces consume the palette (README banner, Mintlify docs theme), and where their config lives.

Include at minimum:

- "Source of truth" section pointing to `assets/branding/palette.json`
- "Logo assets" section pointing to `assets/branding/logo.svg`, `assets/branding/logo-light.svg`, `assets/branding/logo-dark.svg`
- "Docs theming" section referencing `docs/docs.json` and `docs/images/*` as the docs-consumed copies

Also ensure `assets/branding/logo.svg` exists as a canonical logo file (use a copy of `assets/branding/logo-light.svg` if needed).
</action>
<verify>
test -f assets/branding/palette.json
test -f assets/branding/logo.svg
test -f assets/branding/brand-system.md
rg -n "assets/branding/palette\\.json" assets/branding/brand-system.md
rg -n "docs/docs\\.json" assets/branding/brand-system.md
rg -n "#[0-9A-Fa-f]{6}" assets/branding/brand-system.md && exit 1 || true
</verify>
<done>
assets/branding/brand-system.md exists, references assets/branding/palette.json as the single source-of-truth, and does not contain any hex color literals.
</done>
</task>

<task type="auto">
  <name>Task 2: Normalize Mintlify docs branding wiring and sync docs assets</name>
  <files>docs/docs.json, docs/images/logo-light.svg, docs/images/logo-dark.svg, docs/images/favicon.svg</files>
  <action>
Ensure Mintlify docs explicitly reference the v1.15 logo assets and favicon, and sync the docs-consumed copies from `assets/branding/`.

Also ensure `docs/docs.json` palette mapping matches `assets/branding/palette.json`:

- colors.primary == palette.primary
- colors.light == palette.primary
- colors.dark == palette.accent
- background.color.light == palette.secondary
- background.color.dark == palette.background

Required wiring:

- `docs/docs.json.logo.light` == "/images/logo-light.svg"
- `docs/docs.json.logo.dark` == "/images/logo-dark.svg"

Favicon:

- Convert `docs/docs.json.favicon` to an object:
  - favicon.light == "/images/favicon.svg"
  - favicon.dark == "/images/favicon.svg"

Asset sync:

- Ensure these files are byte-equal after sync:
  - docs/images/logo-light.svg == assets/branding/logo-light.svg
  - docs/images/logo-dark.svg == assets/branding/logo-dark.svg
  - docs/images/favicon.svg == assets/branding/favicon.svg

Note: Favicon "derived from the new logo" refinement is tracked later in the milestone, but Phase 20 still enforces consistent wiring and keeps the existing favicon asset in sync.

Implementation constraints:

- Do not add new dependencies.
- If palette or docs mapping is wrong, fix it in-place (Phase 20 must deliver consistent palette application across README + docs).
  </action>
  <verify>
  test -f assets/branding/favicon.svg
  node -e 'const fs=require("fs"); const docs=JSON.parse(fs.readFileSync("docs/docs.json","utf8")); const pal=JSON.parse(fs.readFileSync("assets/branding/palette.json","utf8")); const ok=(x,m)=>{if(!x){console.error(m); process.exitCode=1}}; ok(pal.background==="#141015","palette.background mismatch"); ok(pal.surface==="#1C346D","palette.surface mismatch"); ok(pal.primary==="#18AAA7","palette.primary mismatch"); ok(pal.secondary==="#F5ECD9","palette.secondary mismatch"); ok(pal.accent==="#C1263B","palette.accent mismatch"); ok(docs.colors?.primary===pal.primary,"docs.colors.primary mismatch"); ok(docs.colors?.light===pal.primary,"docs.colors.light mismatch"); ok(docs.colors?.dark===pal.accent,"docs.colors.dark mismatch"); ok(docs.background?.color?.light===pal.secondary,"docs.background.color.light mismatch"); ok(docs.background?.color?.dark===pal.background,"docs.background.color.dark mismatch"); ok(docs.logo?.light==="/images/logo-light.svg","docs.logo.light mismatch"); ok(docs.logo?.dark==="/images/logo-dark.svg","docs.logo.dark mismatch"); ok(typeof docs.favicon==="object","docs.favicon must be object"); ok(docs.favicon?.light==="/images/favicon.svg","docs.favicon.light mismatch"); ok(docs.favicon?.dark==="/images/favicon.svg","docs.favicon.dark mismatch"); if(process.exitCode) process.exit(1); console.log("docs/docs.json brand mapping OK");'
  cmp -s docs/images/logo-light.svg assets/branding/logo-light.svg
  cmp -s docs/images/logo-dark.svg assets/branding/logo-dark.svg
  cmp -s docs/images/favicon.svg assets/branding/favicon.svg
  </verify>
  <done>
  docs/docs.json references the expected logo+favicon assets, and docs/images/ assets are synced (byte-equal) to assets/branding/.
  </done>
  </task>

<task type="auto">
  <name>Task 3: Add a README pointer to the brand system source-of-truth</name>
  <files>README.md</files>
  <action>
Add a small pointer in README to `assets/branding/brand-system.md` so users can find the canonical palette token file.

Also ensure the README logo block references the v1.15 logo assets:

- assets/branding/logo-light.svg
- assets/branding/logo-dark.svg

Constraints:

- Keep the README change minimal (Phase 24 will overhaul the README).
- Do NOT copy/paste palette hex values into README.

Recommendation:

- Add 1-2 lines under the existing banner/logo block, e.g. "Brand tokens live in ...".
  </action>
  <verify>
  rg -n "assets/branding/brand-system\\.md" README.md
  rg -n "assets/branding/logo-light\\.svg" README.md
  rg -n "assets/branding/logo-dark\\.svg" README.md
  </verify>
  <done>
  README contains a direct reference to assets/branding/brand-system.md and references the v1.15 light/dark logo assets.
  </done>
  </task>

<task type="auto">
  <name>Task 4: Update STATE palette decision to the v1.15 palette</name>
  <files>.planning/STATE.md</files>
  <action>
Update the existing palette decision entry so it no longer claims the old palette is current.

Constraints:

- Only adjust the palette decision row(s); do not rewrite unrelated decisions.
- The updated text must reflect the v1.15 palette token names (background/surface/primary/secondary/accent) and point at `assets/branding/palette.json`.
  </action>
  <verify>
  rg -n "assets/branding/palette\\.json" .planning/STATE.md
  rg -n "background" .planning/STATE.md
  rg -n "surface" .planning/STATE.md
  </verify>
  <done>
  .planning/STATE.md no longer advertises the pre-v1.15 palette as the current palette.
  </done>
  </task>

</tasks>

<verification>
- `rg -n "#0F172A|#06B6D4|#F59E0B" README.md .planning/STATE.md assets/branding/brand-system.md` returns no matches (old palette no longer referenced in these docs)
- `test -f assets/branding/logo-light.svg && test -f assets/branding/logo-dark.svg` (logo assets exist and are referenced by README)
- `rg -n "\"logo\"" docs/docs.json && rg -n "\"favicon\"" docs/docs.json` (docs wiring exists)
- `rg -n "#0F172A|#06B6D4|#F59E0B" README.md docs/docs.json docs/images assets/branding .planning/STATE.md` returns no matches (no stale old-palette hexes in active brand consumers)
- `rg -n "#18AAA7" assets/branding/logo-light.svg assets/branding/logo-dark.svg` matches (logo assets contain the v1.15 primary)
</verification>

<success_criteria>

- `assets/branding/palette.json` is the documented palette source-of-truth
- README and planning docs reference the canonical palette without duplicating values
  </success_criteria>

<output>
After completion, create `.planning/phases/20-brand-system-integration/20-01-SUMMARY.md`
</output>
