---
phase: 02-core-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/executor/step-executor.ts
  - packages/core/test/config.test.ts
  - packages/cli/test/commands.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Wait steps can express both plain-text and regex matching through validated config"
    - "Regex wait configuration is compiled to RegExp before calling session.waitForText"
    - "Invalid regex definitions fail validation with actionable config issues"
    - "Generated JSON Schema and init-persisted schema include regex wait structure for IDE completion"
  artifacts:
    - path: "packages/core/src/config/schema.ts"
      provides: "Wait step contract that supports text and regex pattern modes"
      exports: ["TuireelStep", "stepSchema"]
    - path: "packages/core/src/executor/step-executor.ts"
      provides: "Wait step dispatch that compiles config regex pattern payloads"
      exports: ["executeSteps"]
    - path: "packages/core/test/config.test.ts"
      provides: "Validation + schema regression coverage for wait text/regex modes"
    - path: "packages/cli/test/commands.test.ts"
      provides: "Init schema artifact parity checks for regex wait structure"
  key_links:
    - from: "packages/core/src/config/schema.ts"
      to: "packages/core/src/config/parser.ts"
      via: "configSchema.safeParse(parsedConfig)"
      pattern: "configSchema|safeParse"
    - from: "packages/core/src/executor/step-executor.ts"
      to: "packages/core/src/executor/steps/wait.ts"
      via: "waitStep(session, compiledPattern, timeout)"
      pattern: "waitStep\(session"
    - from: "packages/cli/test/commands.test.ts"
      to: "packages/cli/src/commands/init.ts"
      via: "schema.json assertions for wait pattern variants"
      pattern: "schema.json|wait"
---

<objective>
Close the remaining Phase 2 verification gap on STEP-03 by making regex waits first-class in config and execution.

Purpose: align the config contract, executor behavior, and generated schema so wait steps can target both exact text and regex patterns with regression protection.
Output: validated wait regex config path, executor regex compilation, and test coverage proving text+regex parity.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-pipeline/02-core-pipeline-VERIFICATION.md
@.planning/phases/02-core-pipeline/02-01-SUMMARY.md
@.planning/phases/02-core-pipeline/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend wait-step config contract for regex support</name>
  <files>packages/core/src/config/schema.ts</files>
  <action>
    Update the wait-step schema so `pattern` supports both:
    - text mode (existing behavior): `"pattern": "Ready"`
    - regex mode (new behavior): `"pattern": { "regex": "Ready|Done", "flags": "i" }`

    Requirements:
    - Preserve backward compatibility for existing string `pattern` configs.
    - Validate regex payload shape (`regex` non-empty string, `flags` optional string).
    - Reject invalid regex definitions during validation (for example malformed pattern or invalid flags).
    - Keep exported `TuireelStep` typing aligned with schema output.
  </action>
  <verify>
    `pnpm --filter @tuireel/core exec tsc --noEmit`
  </verify>
  <done>
    Config validation accepts both wait text and wait regex modes and rejects invalid regex payloads before execution.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compile wait regex config into runtime matcher</name>
  <files>packages/core/src/executor/step-executor.ts</files>
  <action>
    Add wait-pattern compilation inside step dispatch:
    - If `step.pattern` is a string, pass through unchanged.
    - If `step.pattern` is a regex object, construct `new RegExp(step.pattern.regex, step.pattern.flags)` and pass that RegExp to `waitStep`.

    Requirements:
    - Keep `waitStep()` API usage (`string | RegExp`) unchanged.
    - Surface invalid runtime conversion errors with existing step context wrapper (`Step {index} (wait) failed: ...`).
    - Keep launch/type/press/pause behavior unchanged.
  </action>
  <verify>
    `pnpm --filter @tuireel/core exec tsc --noEmit`
  </verify>
  <done>
    Wait step execution supports both text and regex matchers from validated config without regressing other step dispatch paths.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add regression coverage for wait text+regex schema parity</name>
  <files>
    packages/core/test/config.test.ts
    packages/cli/test/commands.test.ts
  </files>
  <action>
    Extend tests to enforce STEP-03 parity and prevent drift:
    - In core config tests:
      - assert wait step accepts plain string pattern.
      - assert wait step accepts regex object pattern with flags.
      - assert invalid regex object payloads fail with actionable issues.
      - assert generated JSON Schema includes wait pattern alternatives (string + regex object shape).
    - In CLI init integration test:
      - assert persisted `schema.json` includes the same wait regex pattern structure.

    Keep assertions focused on observable contract behavior, not internal implementation details.
  </action>
  <verify>
    `pnpm --filter @tuireel/core test`
    `pnpm --filter tuireel test`
  </verify>
  <done>
    Automated tests enforce wait text+regex behavior across parser and init-generated schema artifacts.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @tuireel/core test
pnpm --filter tuireel test
pnpm --filter @tuireel/core exec tsc --noEmit

# Optional manual contract sanity
node packages/cli/dist/index.js init --force --output /tmp/tuireel-wait-gap.jsonc
```
</verification>

<success_criteria>
- `wait` steps support plain text and regex patterns through validated config
- Invalid regex definitions are blocked at validation time
- Step executor compiles regex wait config to RegExp and dispatches successfully
- Core + CLI tests enforce schema parity for wait text+regex modes
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pipeline/02-03-SUMMARY.md`
</output>
