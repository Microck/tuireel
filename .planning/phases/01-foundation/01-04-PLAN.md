---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/src/commands/validate.ts
  - packages/cli/src/commands/stubs.ts
  - packages/cli/src/index.ts
  - packages/cli/test/commands.test.ts
autonomous: true

must_haves:
  truths:
    - "tuireel init creates .tuireel.jsonc with $schema reference and valid starter config"
    - "tuireel validate on init-generated config reports zero errors"
    - "tuireel validate on malformed config prints clear error messages with field paths"
    - "tuireel record/preview/composite print 'not yet implemented' stubs"
    - "tuireel --help lists all commands"
  artifacts:
    - path: "packages/cli/src/commands/init.ts"
      provides: "init command implementation"
      exports: ["registerInitCommand"]
    - path: "packages/cli/src/commands/validate.ts"
      provides: "validate command implementation"
      exports: ["registerValidateCommand"]
    - path: "packages/cli/src/commands/stubs.ts"
      provides: "Stub commands for record, preview, composite"
  key_links:
    - from: "packages/cli/src/commands/init.ts"
      to: "@tuireel/core"
      via: "import generateJsonSchema"
      pattern: "generateJsonSchema"
    - from: "packages/cli/src/commands/validate.ts"
      to: "@tuireel/core"
      via: "import loadConfig/validateConfig"
      pattern: "loadConfig|validateConfig"
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/commands/"
      via: "register commands on program"
      pattern: "register.*Command"
---

<objective>
Wire CLI commands: init (scaffold config), validate (check config), and stubs for future commands.

Purpose: Completes all Phase 1 user-facing requirements (CLI-05, CLI-06).
Output: Working tuireel init and tuireel validate commands.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement init and validate commands</name>
  <files>packages/cli/src/commands/init.ts, packages/cli/src/commands/validate.ts, packages/cli/src/commands/stubs.ts</files>
  <action>
  init.ts:
  - Register "init" command on Commander program
  - Option: --output / -o (default ".tuireel.jsonc")
  - Check if file exists, warn if overwriting (--force to overwrite)
  - Generate JSON Schema file to ~/.tuireel/schema.json using generateJsonSchema() from @tuireel/core
  - Write starter .tuireel.jsonc with: comment header explaining the file, $schema pointing to the generated schema, output "demo.mp4", fps 30, cols 80, rows 24, example steps (launch "echo hello", pause 1)
  - Print success message with next steps hint

  validate.ts:
  - Register "validate" command on Commander program
  - Argument: config file path (default ".tuireel.jsonc")
  - Call loadConfig() from @tuireel/core
  - On success: print "Config is valid" with green checkmark
  - On Zod error: format each issue as "field.path: message" with red X, exit code 1
  - On JSONC parse error: print parse error with line/column info, exit code 1

  stubs.ts:
  - Register record, preview, composite commands as stubs
  - Each prints "Not yet implemented. Coming in Phase N." and exits cleanly
  </action>
  <verify>
  `pnpm build && node packages/cli/dist/index.js init --force && node packages/cli/dist/index.js validate .tuireel.jsonc`
  </verify>
  <done>init creates valid config, validate passes on it, validate fails clearly on bad input.</done>
</task>

<task type="auto">
  <name>Task 2: Wire commands into CLI and add tests</name>
  <files>packages/cli/src/index.ts, packages/cli/test/commands.test.ts</files>
  <action>
  index.ts:
  - Import and call all register*Command functions, passing the Commander program
  - Ensure --help lists: init, validate, record, preview, composite

  commands.test.ts:
  - Test: init writes .tuireel.jsonc with $schema field and valid JSON
  - Test: validate on init output exits 0
  - Test: validate on invalid config (missing steps) exits 1 with error message containing "steps"
  - Test: validate on malformed JSONC (syntax error) exits 1
  - Use tmp directory for test file I/O (node:os tmpdir)
  - Test: --help output contains all command names
  </action>
  <verify>`pnpm --filter tuireel test && pnpm build`</verify>
  <done>All CLI commands registered, tests pass for init/validate happy+error paths.</done>
</task>

</tasks>

<verification>
```bash
pnpm build
pnpm --filter tuireel test
# End-to-end:
node packages/cli/dist/index.js init --force
node packages/cli/dist/index.js validate .tuireel.jsonc
# Should print "valid"
echo '{"invalid": true}' > /tmp/bad.jsonc
node packages/cli/dist/index.js validate /tmp/bad.jsonc
# Should print error about missing "steps"
```
</verification>

<success_criteria>
- tuireel init creates .tuireel.jsonc with $schema reference
- tuireel validate on init output reports zero errors
- tuireel validate on bad config shows field path + error message
- tuireel --help lists all 5 commands
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
