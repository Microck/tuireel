---
phase: 01-foundation
plan: 05
type: execute
wave: 4
depends_on: ["01-02", "01-04"]
files_modified:
  - packages/core/src/config/generate-schema.ts
  - packages/core/test/config.test.ts
  - packages/cli/test/commands.test.ts
autonomous: true

must_haves:
  truths:
    - "generateJsonSchema() emits a non-empty TuireelConfig schema with output/fps/cols/rows/steps properties"
    - "Generated schema encodes step variants for launch/type/press/wait/pause so IDE autocomplete can suggest step fields"
    - "tuireel init writes schema.json that mirrors the runtime config contract (not an empty object definition)"
    - "Tests fail if schema generation regresses to empty or loses required config fields"
  artifacts:
    - path: "packages/core/src/config/generate-schema.ts"
      provides: "Schema generation implementation without empty output regression"
      exports: ["generateJsonSchema"]
    - path: "packages/core/test/config.test.ts"
      provides: "Schema structure regression tests"
    - path: "packages/cli/test/commands.test.ts"
      provides: "init command integration test asserting written schema file contents"
  key_links:
    - from: "packages/core/src/config/generate-schema.ts"
      to: "packages/core/src/config/schema.ts"
      via: "configSchema import"
      pattern: "configSchema"
    - from: "packages/cli/src/commands/init.ts"
      to: "@tuireel/core"
      via: "generateJsonSchema"
      pattern: "generateJsonSchema"
---

<objective>
Close the Phase 1 verification gap blocking CLI-02 by making generated JSON Schema actually describe Tuireel config fields and step variants.

Purpose: Restore schema-driven IDE autocomplete and keep schema output aligned with config validation rules.
Output: A non-empty generated schema contract plus regression tests proving parity and preventing future drift.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-foundation-VERIFICATION.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair JSON Schema generation output</name>
  <files>packages/core/src/config/generate-schema.ts</files>
  <action>
  Replace the current schema-generation path that yields `TuireelConfig: {}` with a generation strategy that emits concrete config fields and step variant structure.

  Requirements:
  - Remove the unsafe compatibility cast (`configSchema as never`) that currently hides mismatch issues.
  - Ensure output schema includes top-level `type: "object"`, `properties`, and `required` entries for runtime-required fields (`steps`) while preserving defaults (`output`, `fps`, `cols`, `rows`).
  - Ensure `steps.items` contains variant-specific shape for launch/type/press/wait/pause.
  - Preserve `$schema` declaration in returned schema object.

  Implementation note:
  - Prefer a single-source approach derived from existing config schema definitions (not a disconnected hand-written contract).
  </action>
  <verify>
  `pnpm --filter @tuireel/core test`
  </verify>
  <done>
  generateJsonSchema() emits a usable object schema for IDEs and no longer returns an empty TuireelConfig definition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add core schema regression tests for contract shape</name>
  <files>packages/core/test/config.test.ts</files>
  <action>
  Strengthen schema tests so the gap cannot regress silently:
  - Assert generated schema contains non-empty `properties` with keys: `output`, `fps`, `cols`, `rows`, `steps`.
  - Assert schema marks `steps` as required and `steps.items` exposes step variant discriminators including `type` literals.
  - Keep existing draft declaration assertion and add focused shape checks for step-specific fields (for example launch `command`, type `text`, wait `pattern`, pause `duration`).
  </action>
  <verify>
  `pnpm --filter @tuireel/core test`
  </verify>
  <done>
  Core tests enforce schema usability, not only schema metadata presence.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extend init command integration test to validate persisted schema file</name>
  <files>packages/cli/test/commands.test.ts</files>
  <action>
  Expand CLI init integration test coverage:
  - Read `${TUIREEL_HOME}/schema.json` after `init` execution.
  - Assert persisted schema includes concrete config properties and step variant structure.
  - Keep existing config file assertions (`$schema` presence + validateConfig success).

  This confirms end-to-end behavior for CLI-02 from generation to persisted schema artifact.
  </action>
  <verify>
  `pnpm --filter tuireel test`
  </verify>
  <done>
  init test validates both generated config and generated schema artifact.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @tuireel/core test
pnpm --filter tuireel test
pnpm build

# Manual sanity check (optional)
node packages/cli/dist/index.js init --force --output /tmp/tuireel-gap-check.jsonc
cat ~/.tuireel/schema.json
```
</verification>

<success_criteria>
- generateJsonSchema no longer returns empty `TuireelConfig`
- Schema includes top-level config fields and step variants
- CLI init integration test asserts schema artifact structure
- Core + CLI tests pass and protect against regression
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
