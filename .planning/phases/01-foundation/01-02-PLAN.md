---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/config/parser.ts
  - packages/core/src/config/generate-schema.ts
  - packages/core/src/config/index.ts
  - packages/core/src/index.ts
  - packages/core/test/config.test.ts
autonomous: true

must_haves:
  truths:
    - "Valid JSONC config (with comments) parses without errors"
    - "Invalid config produces clear, actionable Zod error messages"
    - "Generated JSON Schema validates the same configs Zod validates"
    - "$schema field is accepted but not required"
  artifacts:
    - path: "packages/core/src/config/schema.ts"
      provides: "Zod config schema (configSchema, stepSchema)"
      exports: ["configSchema", "stepSchema", "TuireelConfig"]
    - path: "packages/core/src/config/parser.ts"
      provides: "JSONC loading + Zod validation"
      exports: ["loadConfig", "validateConfig"]
    - path: "packages/core/src/config/generate-schema.ts"
      provides: "JSON Schema generation from Zod"
      exports: ["generateJsonSchema"]
    - path: "packages/core/test/config.test.ts"
      provides: "Config validation tests"
  key_links:
    - from: "packages/core/src/config/parser.ts"
      to: "packages/core/src/config/schema.ts"
      via: "import configSchema"
      pattern: "configSchema\\.parse|configSchema\\.safeParse"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/config/index.ts"
      via: "re-export"
      pattern: 'export.*from.*config'
---

<objective>
Implement config schema (Zod), JSONC parser, and JSON Schema generation with TDD.

Purpose: Config system is used by init, validate, and all future commands.
Output: Tested config module in @tuireel/core that parses JSONC, validates with Zod, generates JSON Schema.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<feature>
  <name>Config Schema and Parser</name>
  <files>packages/core/src/config/schema.ts, packages/core/src/config/parser.ts, packages/core/src/config/generate-schema.ts, packages/core/test/config.test.ts</files>
  <behavior>
  Test cases (RED first, then GREEN):
  1. Valid minimal config (just steps array with one launch step) -> parses successfully
  2. Config with JSONC comments -> parses successfully (comments stripped)
  3. Config with $schema field -> accepted, not in validated output
  4. Config missing required "steps" -> Zod error mentioning "steps"
  5. Config with invalid step type -> Zod error mentioning valid types
  6. Config with defaults -> output.default="output.mp4", fps.default=30, cols=80, rows=24
  7. generateJsonSchema() -> returns valid JSON Schema object with $schema draft ref

  Schema definition (from research):
  - stepSchema: discriminatedUnion on "type" with launch, type, press, wait, pause
  - configSchema: $schema optional string, output default "output.mp4", fps default 30, cols default 80, rows default 24, steps array min 1
  - Types exported: TuireelConfig = z.infer<typeof configSchema>
  </behavior>
  <implementation>
  1. schema.ts: Define stepSchema and configSchema with Zod. Export types.
  2. parser.ts: loadConfig(path) reads file, parseJsonc, configSchema.safeParse. validateConfig(raw) for string input. Return typed result or structured errors with path + message.
  3. generate-schema.ts: Use zod-to-json-schema to convert configSchema. Export function.
  4. config/index.ts: barrel export all public API
  5. Update packages/core/src/index.ts to re-export config module
  6. Add vitest to root devDeps, configure in packages/core
  </implementation>
</feature>

<verification>
```bash
pnpm --filter @tuireel/core test
# All 7+ test cases pass
pnpm build
```
</verification>

<success_criteria>
- All config tests pass (valid parsing, error cases, defaults, JSON Schema generation)
- @tuireel/core exports loadConfig, validateConfig, configSchema, generateJsonSchema, TuireelConfig
- Build succeeds with types
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
