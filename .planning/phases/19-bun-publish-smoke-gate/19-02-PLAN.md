---
phase: 19-bun-publish-smoke-gate
plan: 02
type: execute
wave: 2
depends_on:
  - "19-01"
files_modified:
  - scripts/publish-smoke.ts
autonomous: true

must_haves:
  truths:
    - "Bun smoke fails fast if installed tuireel/@tuireel/core package.json does not match the packed tarball package.json (tarball equivalence)"
    - "Bun smoke fails fast if a nested node_modules/tuireel/node_modules/@tuireel/core exists after install"
    - "Bun smoke proves the installed @tuireel/core export surface includes resolveOutputPath (Phase 17 contract)"
    - "Temp directories created by publish smoke are cleaned up (including bunDir)"
  artifacts:
    - path: "scripts/publish-smoke.ts"
      provides: "Bun determinism assertions + export-surface regression check"
      contains: "resolveOutputPath"
  key_links:
    - from: "scripts/publish-smoke.ts"
      to: "Packed tarballs on disk"
      via: "tar -xOzf <tarPath> package/package.json + compare to installed node_modules package.json"
      pattern: "tar\\s+-xOzf"
    - from: "scripts/publish-smoke.ts"
      to: "packages/core export contract"
      via: "bun runs a temp .mjs import check"
      pattern: "resolveOutputPath"
---

<objective>
Add regression guards to the Bun publish smoke so the original failure mode (nested registry core lacking exports) is detected deterministically and cleaned up reliably.

Purpose: Make the smoke gate trustworthy and self-diagnosing for Bun resolution issues.
Output: Assertions for nested core absence, core export surface check, and complete temp cleanup.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bun-publish-smoke-gate/19-RESEARCH.md

@scripts/publish-smoke.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Bun determinism assertions (tarball equivalence + no nested core)</name>
  <files>scripts/publish-smoke.ts</files>
  <action>
After `bun install` succeeds in the Bun smoke section, add TWO determinism assertions that fail the smoke test with actionable messages.

Tarball equivalence assertion (REQUIRED):

- Read the packed tarball package.json payloads:
  - `tar -xOzf "${coreTarPath}" package/package.json`
  - `tar -xOzf "${cliTarPath}" package/package.json`
- Read the installed package.json files from Bun's temp project:
  - `${bunDir}/node_modules/@tuireel/core/package.json`
  - `${bunDir}/node_modules/tuireel/package.json`
- Assert that each installed package.json is equivalent to its tarball's `package/package.json`.
  - Required comparison: parse both as JSON and compare `name` + `version`.
  - Required comparison: compare `exports` by keys and values:
    - Compare `Object.keys(exports).sort().join('\n')` for tarball vs installed
    - For each export key, compare `JSON.stringify(exports[key])` for tarball vs installed
- On failure, fail the smoke test and exit non-zero.
- Error message must explicitly state that Bun may have resolved a package from the registry/cache rather than from the packed tarball under test, and print:
  - expected tarball path
  - installed package.json path
  - expected vs actual `name`/`version` at minimum

Nested install assertion:

- If `node_modules/tuireel/node_modules/@tuireel/core` exists, fail the smoke test with an actionable message and exit non-zero.

This assertion must run BEFORE executing the CLI, so the error clearly identifies the resolution bug.
</action>
<verify>
pnpm -w publish:smoke
</verify>
<done>The smoke script fails fast if Bun installed package.json metadata does not match the packed tarballs (tuireel + @tuireel/core) or if Bun produced a nested `tuireel/node_modules/@tuireel/core` tree.</done>
</task>

<task type="auto">
  <name>Task 2: Add Bun export-surface regression check + complete cleanup</name>
  <files>scripts/publish-smoke.ts</files>
  <action>
Add two hardening improvements to the Bun smoke section.

Export-surface check (Phase 17 contract):

- Write a tiny ESM file in `bunDir` (for explicit, bun-version-stable ESM mode) that proves the installed `@tuireel/core` includes the Phase 17 export:
  - File path: `${bunDir}/core-export-surface.mjs`
  - File contents:
    - `import { resolveOutputPath } from '@tuireel/core';`
    - `console.log(typeof resolveOutputPath);`
- Run it with Bun:
  - `bun --cwd "${bunDir}" core-export-surface.mjs`
- Treat any failure as a smoke failure and include the stderr snippet in `fail(...)`.

Ordering (diagnostic value):

- Run the export-surface check BEFORE executing the CLI (`bun --cwd "${bunDir}" x --no-install tuireel ...`).

Cleanup:

- Ensure `bunDir` is deleted in the script's cleanup block alongside `packDir` and `npxDir`.
- Keep cleanup best-effort (do not throw if deletion fails).
  </action>
  <verify>
  pnpm -w publish:smoke
  </verify>
  <done>`pnpm -w publish:smoke` passes, and on failure the bun smoke provides a deterministic, self-diagnosing error (nested core or missing resolveOutputPath) while always cleaning up temp dirs.</done>
  </task>

</tasks>

<verification>
- `pnpm -w publish:smoke` passes locally.
- Bun smoke asserts installed `tuireel` and `@tuireel/core` package.json content matches the packed tarballs under test.
- Bun smoke asserts that `node_modules/tuireel/node_modules/@tuireel/core` does not exist.
- Bun smoke validates `resolveOutputPath` is importable from `@tuireel/core` by running `${bunDir}/core-export-surface.mjs` with Bun.
- All temp dirs created by the script are removed at the end (packDir, npxDir, bunDir).
</verification>

<success_criteria>

- REL-04 gate restored: Bun smoke deterministically tests the packed tarballs (no nested/registry mismatch) and proves CLI + core import surface is correct.
- `.github/workflows/release.yml` remains unchanged and continues to run `pnpm -w publish:smoke` before publish.
  </success_criteria>

<output>
After completion, create `.planning/phases/19-bun-publish-smoke-gate/19-02-SUMMARY.md`
</output>
