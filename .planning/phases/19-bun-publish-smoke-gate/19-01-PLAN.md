---
phase: 19-bun-publish-smoke-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/publish-smoke.ts
autonomous: true

must_haves:
  truths:
    - "pnpm -w publish:smoke runs a Bun smoke install that resolves tuireel + @tuireel/core from the packed tarballs"
    - "Bun smoke pins @tuireel/core transitively via package.json overrides so tuireel's dependency graph uses the packed core tarball"
    - "Bun smoke executes tuireel via Bun (bun x --no-install), exercising Bun conditional exports and validating bin wiring"
  artifacts:
    - path: "scripts/publish-smoke.ts"
      provides: "Deterministic bun tarball install + bun runtime execution smoke"
      contains: "bun install"
  key_links:
    - from: "scripts/publish-smoke.ts"
      to: "@tuireel/core tarball"
      via: "bun temp project package.json overrides"
      pattern: '"overrides"'
    - from: "scripts/publish-smoke.ts"
      to: "tuireel CLI entrypoint"
      via: "bun --cwd <dir> x --no-install tuireel"
      pattern: "bun\\s+--cwd.*\\sx\\s+--no-install"
---

<objective>
Make the Bun section of `pnpm -w publish:smoke` deterministic by pinning Bun resolution of `@tuireel/core` to the packed core tarball via overrides, and execute the CLI with the Bun runtime.

Purpose: Close REL-04 regression where Bun installs a nested registry-resolved core lacking Phase 17 exports.
Output: Bun smoke uses a temp manifest + overrides and runs `--help/--version` via Bun.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-bun-publish-smoke-gate/19-RESEARCH.md

@.planning/phases/16-publish-installability-fixes/16-02-SUMMARY.md
@.planning/phases/17-fix-multi-format-record-outputs/17-01-SUMMARY.md

@scripts/publish-smoke.ts
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Bun "bun add" smoke with a temp manifest + overrides + bun install</name>
  <files>scripts/publish-smoke.ts</files>
  <action>
Rewrite ONLY the Bun section to install from a controlled `package.json` rather than `bun add`, so transitive core resolution is pinned to the packed tarball via overrides.

Implementation details (use the tarball absolute paths already computed in the script):

- Create `bunDir` as today.
- Do NOT run `bun init -y`.
- Write a minimal `package.json` into `bunDir` with:
  - `private: true`
  - `type: "module"`
  - `dependencies`:
    - `"tuireel": <abs cliTarPath>`
    - `"@tuireel/core": <abs coreTarPath>`
  - `overrides`:
    - `"@tuireel/core": <abs coreTarPath>`
- Run `bun install` (not `bun add`) in `bunDir`.

Constraints:

- Keep the Bun section optional if `bun` is not in PATH (behavior unchanged).
- Do not add new dependencies to the repo.
- Keep the rest of the smoke script unchanged (pack, workspace: check, npx smoke).
  </action>
  <verify>
  command -v bun >/dev/null && bun --version || true
  pnpm -w publish:smoke
  </verify>
  <done>Bun smoke installs using `bun install` from a temp `package.json` that pins `@tuireel/core` to the packed core tarball via `overrides`.</done>
  </task>

<task type="auto">
  <name>Task 2: Execute tuireel via bun x (bin wiring + Bun runtime)</name>
  <files>scripts/publish-smoke.ts</files>
  <action>
In the Bun smoke section, execute the CLI through Bun's binary runner to validate the bin shim wiring and keep the runtime as Bun.

Run exactly:

- `bun --cwd "${bunDir}" x --no-install tuireel --help`
- `bun --cwd "${bunDir}" x --no-install tuireel --version`

Keep the same pass/fail messages, but update them to reflect bun runtime.
</action>
<verify>
pnpm -w publish:smoke
</verify>
<done>`pnpm -w publish:smoke` passes locally with Bun installed, and Bun smoke runs `--help` and `--version` using Bun runtime.</done>
</task>

</tasks>

<verification>
- `pnpm -w publish:smoke` passes locally with Bun installed (Bun section runs).
- In the Bun temp dir, the CLI is executed via `bun --cwd ... x --no-install tuireel` (covers bin wiring + Bun runtime).
- Bun installs are constrained to the packed tarballs by using a temp manifest with `overrides` for `@tuireel/core`.
</verification>

<success_criteria>

- REL-04 regression fixed at the gate: Bun smoke can no longer accidentally test a registry-resolved `@tuireel/core` instead of the packed tarball under test.
- Release workflow remains gated on `pnpm -w publish:smoke` and would pass in CI (Bun installed via setup-bun).
  </success_criteria>

<output>
After completion, create `.planning/phases/19-bun-publish-smoke-gate/19-01-SUMMARY.md`
</output>
