---
phase: 03-output-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/encoding/encoder.ts
  - packages/core/src/encoding/encoder-profiles.ts
  - packages/core/src/encoding/gif-encoder.ts
autonomous: true

must_haves:
  truths:
    - "`tuireel record --format webm` produces a playable VP9 WebM file"
    - "`tuireel record --format gif` produces an optimized GIF using two-pass palettegen"
    - "GIF output is capped at 10-15fps regardless of recording FPS"
    - "Format selection works via CLI flag and config field"
  artifacts:
    - path: "packages/core/src/encoding/encoder-profiles.ts"
      provides: "Format-specific ffmpeg argument profiles"
      contains: "libvpx-vp9"
    - path: "packages/core/src/encoding/gif-encoder.ts"
      provides: "Two-pass GIF encoding (palettegen + paletteuse)"
      contains: "palettegen"
  key_links:
    - from: "packages/core/src/encoding/encoder-profiles.ts"
      to: "packages/core/src/encoding/encoder.ts"
      via: "profile lookup by format"
    - from: "packages/core/src/encoding/gif-encoder.ts"
      to: "ffmpeg binary"
      via: "two sequential ffmpeg spawns (palette then encode)"
---

<objective>
Add WebM (VP9) and GIF (two-pass palettegen) encoding to the existing ffmpeg pipeline.

Purpose: Deliver all three output formats required for v1 (MP4 already exists from Phase 2).
Output: WebM and GIF encoder profiles integrated into the recording pipeline.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-output-quality/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebM VP9 encoder profile + format selection</name>
  <files>
    packages/core/src/encoding/encoder-profiles.ts
    packages/core/src/encoding/encoder.ts
  </files>
  <action>
  - Create encoding/encoder-profiles.ts with a `Record<OutputFormat, EncoderProfile>` mapping
  - Move existing MP4 args (`libx264`, `yuv420p`, `preset fast`, `crf 23`) into the profiles map
  - Add WebM profile: `-c:v libvpx-vp9 -crf 30 -b:v 0 -pix_fmt yuv420p`
  - Add color space flags for consistency: `-colorspace bt709 -color_primaries bt709 -color_trc bt709`
  - Update encoding/encoder.ts to look up profile by format instead of hardcoded MP4 args
  - Ensure `--format` CLI flag maps to the profile (mp4 | webm | gif)
  - Add `format` field to config schema if not already present
  </action>
  <verify>
  - `tuireel record --format webm` with a simple test config produces a .webm file
  - `ffprobe output.webm` shows VP9 codec
  - `tuireel record --format mp4` still works (no regression)
  </verify>
  <done>WebM VP9 encoding works. Format selection via CLI and config. MP4 unbroken.</done>
</task>

<task type="auto">
  <name>Task 2: GIF two-pass palettegen encoder</name>
  <files>
    packages/core/src/encoding/gif-encoder.ts
    packages/core/src/encoding/encoder.ts
    packages/core/src/encoding/encoder-profiles.ts
  </files>
  <action>
  - Create encoding/gif-encoder.ts handling two-pass GIF encoding:
    - Pass 1: `ffmpeg -i input -vf "fps=15,palettegen=stats_mode=diff" palette.png` (temp file)
    - Pass 2: `ffmpeg -i input -i palette.png -lavfi "fps=15[x];[x][1:v]paletteuse=dither=bayer" output.gif`
  - Cap GIF fps at 10-15 (override recording fps). Default 15.
  - Clean up temp palette.png after encoding
  - Register GIF in encoding/encoder-profiles.ts with `twoPass: true` flag
  - In encoding/encoder.ts, detect twoPass flag and delegate to gif-encoder instead of single-pass
  - Consider max-width scaling for size: add optional `-vf scale=WIDTH:-1` before palettegen
  </action>
  <verify>
  - `tuireel record --format gif` produces a .gif file
  - GIF plays correctly in browser/image viewer
  - GIF file size is reasonable (not 50MB+ for a short recording)
  - Temp palette.png is cleaned up after encoding
  </verify>
  <done>GIF uses two-pass palettegen. FPS capped at 15. Temp files cleaned. Reasonable file size.</done>
</task>

</tasks>

<verification>
- All three formats produce valid output: `tuireel record --format mp4|webm|gif`
- `ffprobe` confirms correct codecs (H.264 for MP4, VP9 for WebM)
- GIF has good color quality (no banding) due to palettegen
</verification>

<success_criteria>
- REC-04 satisfied: WebM output with VP9 encoding
- REC-05 satisfied: GIF output with optimized two-pass palette
- No regressions to MP4 pipeline from Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-quality/03-01-SUMMARY.md`
</output>
