---
phase: 03-output-quality
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/themes/built-in.ts
  - packages/core/src/themes/schema.ts
  - packages/core/src/themes/resolve.ts
  - packages/core/src/config/schema.ts
  - packages/core/src/recorder.ts
autonomous: true

must_haves:
  truths:
    - "`theme: 'catppuccin'` in config applies Catppuccin colors to output"
    - "Custom theme object with 16 ANSI colors + fg/bg is accepted and applied"
    - "Invalid theme config (bad hex, missing fields) produces clear validation error"
    - "Themed output has correct colors and padding matching tuistory rendering"
  artifacts:
    - path: "packages/core/src/themes/built-in.ts"
      provides: "8 built-in theme definitions"
      contains: "dracula"
    - path: "packages/core/src/themes/schema.ts"
      provides: "Zod schema for theme validation"
      contains: "themeSchema"
    - path: "packages/core/src/themes/resolve.ts"
      provides: "Theme resolution (name string or custom object)"
      exports: ["resolveTheme"]
  key_links:
    - from: "packages/core/src/themes/resolve.ts"
      to: "tuistory screenshot renderer"
      via: "passing resolved theme config to screenshot options"
    - from: "packages/core/src/config/schema.ts"
      to: "packages/core/src/themes/schema.ts"
      via: "theme field references themeSchema"
---

<objective>
Add terminal theming: 8 built-in themes and custom theme support via config.

Purpose: Let users style terminal output with popular color schemes or their own colors.
Output: Theme registry, Zod schema, resolver, and integration with tuistory rendering.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-output-quality/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme schema, built-in registry, and resolver</name>
  <files>
    packages/core/src/themes/schema.ts
    packages/core/src/themes/built-in.ts
    packages/core/src/themes/resolve.ts
    packages/core/src/themes/index.ts
  </files>
  <action>
  - Create themes/schema.ts with Zod schema:
    - `background`, `foreground`: required hex color strings (`/^#[0-9a-fA-F]{6}$/`)
    - `cursor`: optional hex
    - `colors` object: 16 ANSI colors (black, red, green, yellow, blue, magenta, cyan, white + bright variants)
    - `padding`: optional number
    - `fontFamily`: optional string
    - `fontSize`: optional number
  - Create themes/built-in.ts with 8 themes: Dracula, Catppuccin (Mocha), One Dark, Monokai, Solarized Dark, Tokyo Night, Nord, Gruvbox Dark
    - Each theme has all 16 ANSI colors + fg + bg + cursor
  - Create themes/resolve.ts:
    - `resolveTheme(input: string | ThemeConfig): ThemeConfig`
    - String input → look up in built-in registry (throw if not found)
    - Object input → validate with Zod schema, return
  - Create themes/index.ts barrel export
  </action>
  <verify>
  - Import resolveTheme, call with "catppuccin" → returns Catppuccin config
  - Call with unknown string → throws clear error
  - Call with valid custom object → returns validated object
  - Call with invalid object (bad hex) → throws Zod validation error
  </verify>
  <done>8 built-in themes defined. Custom themes validated. Resolver handles both string and object input.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate theme into config schema and tuistory renderer</name>
  <files>
    packages/core/src/config/schema.ts
    packages/core/src/recorder.ts
  </files>
  <action>
  - Add `theme` field to config schema: `z.union([z.string(), themeSchema]).optional()`
    - String = built-in theme name, Object = custom theme
  - In the recording pipeline (recorder.ts or equivalent), resolve theme before capture loop
  - Pass resolved theme colors to tuistory's screenshot/render options:
    - Map theme background, foreground, and 16 ANSI colors to ghostty-opentui format
    - Pass padding, fontFamily, fontSize if set
  - Default behavior when no theme specified: use tuistory's default rendering (no theme override)
  </action>
  <verify>
  - Config with `"theme": "dracula"` validates and produces themed output
  - Config with custom theme object validates and produces themed output
  - Config without theme field still works (no regression)
  - Colors in output visually match the theme definition
  </verify>
  <done>Theme field in config. Resolved theme passed to tuistory. Default (no theme) works unchanged.</done>
</task>

</tasks>

<verification>
- `tuireel record` with `theme: "catppuccin"` uses Catppuccin colors
- `tuireel record` with custom theme object applies custom colors
- `tuireel validate` catches invalid theme config
- No-theme config produces default output (regression-free)
</verification>

<success_criteria>
- THM-01 satisfied: 8 built-in themes available by name
- THM-02 satisfied: Custom theme objects with full ANSI palette + styling
- Themed output has visual parity with tuistory's standalone rendering
</success_criteria>

<output>
After completion, create `.planning/phases/03-output-quality/03-02-SUMMARY.md`
</output>
