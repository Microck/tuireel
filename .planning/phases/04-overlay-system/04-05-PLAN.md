---
phase: 04-overlay-system
plan: 05
type: execute
wave: 1
depends_on: ["04-04"]
files_modified:
  - packages/core/src/overlay/bezier.ts
  - packages/core/src/overlay/__tests__/bezier.test.ts
  - packages/core/src/overlay/types.ts
  - packages/core/src/overlay/cursor-renderer.ts
  - packages/core/src/overlay/__tests__/cursor-renderer.test.ts
  - packages/core/src/compositor.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Cursor micro-jitter reaches a visible 1-2px range on long moves, peaks mid-path, and decays near start/end frames"
    - "Bezier path endpoints remain exact (start and destination coordinates are unchanged by jitter)"
    - "Cursor renderer uses shared overlay contracts from overlay/types.ts instead of local duplicate types"
    - "Compositor wiring still renders cursor/HUD overlays without behavioral regressions"
  artifacts:
    - path: "packages/core/src/overlay/bezier.ts"
      provides: "Adjusted jitter envelope implementation that matches the planned 1-2px behavior"
      exports: ["computeCursorPath", "moveDuration"]
    - path: "packages/core/src/overlay/__tests__/bezier.test.ts"
      provides: "Regression coverage for jitter magnitude and endpoint decay"
    - path: "packages/core/src/overlay/types.ts"
      provides: "Shared cursor renderer contracts consumed by renderer and compositor"
    - path: "packages/core/src/overlay/cursor-renderer.ts"
      provides: "Cursor raster renderer typed against shared overlay contracts"
    - path: "packages/core/src/overlay/__tests__/cursor-renderer.test.ts"
      provides: "Renderer contract test coverage"
  key_links:
    - from: "packages/core/src/overlay/bezier.ts"
      to: "packages/core/src/overlay/__tests__/bezier.test.ts"
      via: "jitter-envelope assertions for long-distance moves"
      pattern: "computeCursorPath|microJitter"
    - from: "packages/core/src/overlay/cursor-renderer.ts"
      to: "packages/core/src/overlay/types.ts"
      via: "shared cursor config + overlay image contract imports"
      pattern: "CursorConfig|Overlay"
    - from: "packages/core/src/compositor.ts"
      to: "packages/core/src/overlay/cursor-renderer.ts"
      via: "renderCursor() typed integration"
      pattern: "renderCursor|cursorConfig"
---

<objective>
Close the remaining Phase 4 verification gaps: cursor micro-jitter magnitude and shared overlay-contract adoption.

Purpose: fully satisfy OVR-01 quality targets and align overlay renderer contracts so cursor and HUD follow the same shared type layer.
Output: jitter behavior update + regression tests + cursor renderer/type refactor with compositor compatibility.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-overlay-system/04-overlay-system-VERIFICATION.md
@.planning/phases/04-overlay-system/04-02-SUMMARY.md
@.planning/phases/04-overlay-system/04-03-SUMMARY.md
@.planning/phases/04-overlay-system/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise cursor micro-jitter to the planned 1-2px envelope</name>
  <files>packages/core/src/overlay/bezier.ts</files>
  <action>
    Update `microJitter()` so long cursor moves produce visible jitter in the planned range while preserving endpoint stability:
    - Keep bell-curve timing (peak near mid-path, low near endpoints).
    - Replace the current `Math.min(0.4, dist * 0.0004)` cap with a distance-aware envelope that can reach 1-2px on longer moves.
    - Preserve short-move behavior (no jitter for small distances) and exact terminal endpoint overwrite.

    Guardrails:
    - Do not change public function signatures.
    - Keep existing ease curve and step-count behavior unless required for jitter correctness.
  </action>
  <verify>
    `pnpm --filter @tuireel/core test -- src/overlay/__tests__/bezier.test.ts`
  </verify>
  <done>
    Long moves visibly jitter within the intended amplitude band, and jitter naturally fades toward movement boundaries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add jitter regression tests for magnitude bounds and endpoint decay</name>
  <files>packages/core/src/overlay/__tests__/bezier.test.ts</files>
  <action>
    Extend bezier tests to lock the gap closure behavior:
    - Add a long-move test that demonstrates observable jitter amplitude (targeting 1-2px envelope, not 0.4px).
    - Add a fade-shape test showing endpoint jitter remains meaningfully lower than mid-path jitter.
    - Keep tests robust without module mocking by using deterministic geometry where baseline path curvature is minimized.

    Maintain existing endpoint and duration assertions.
  </action>
  <verify>
    `pnpm --filter @tuireel/core test -- src/overlay/__tests__/bezier.test.ts`
  </verify>
  <done>
    Tests fail if jitter regresses below visual target or loses bell-curve decay properties.
  </done>
</task>

<task type="auto">
  <name>Task 3: Adopt shared overlay contracts in cursor renderer and keep compositor wiring green</name>
  <files>
    packages/core/src/overlay/types.ts
    packages/core/src/overlay/cursor-renderer.ts
    packages/core/src/overlay/__tests__/cursor-renderer.test.ts
    packages/core/src/compositor.ts
  </files>
  <action>
    Remove cursor renderer type divergence by routing through shared overlay contracts:
    - Move/define cursor renderer input/output contracts in `overlay/types.ts` (or alias existing shared contracts) so both renderer and compositor consume the same source-of-truth types.
    - Refactor `cursor-renderer.ts` to import those shared contracts instead of declaring local interfaces.
    - Adjust compositor typing/imports as needed without changing runtime behavior.
    - Add focused cursor renderer tests validating contract shape (buffer output + dimensions/default handling).
  </action>
  <verify>
    `pnpm --filter @tuireel/core test -- src/overlay/__tests__/cursor-renderer.test.ts`
    `pnpm --filter @tuireel/core exec tsc --noEmit`
  </verify>
  <done>
    Cursor/HUD overlay type contracts are centrally defined and consumed consistently by renderer/compositor codepaths.
  </done>
</task>

</tasks>

<verification>
```bash
pnpm --filter @tuireel/core test
pnpm --filter @tuireel/core exec tsc --noEmit
pnpm build
```

Manual visual check:
- Re-run `tuireel composite` on a recording with long cursor travel and verify jitter is visible mid-path and subtle at movement boundaries.
</verification>

<success_criteria>
- Phase 4 verification gap #1 closes: jitter target is visibly 1-2px with endpoint decay
- Phase 4 verification gap #2 closes: cursor renderer consumes shared overlay contracts
- Core tests and type-checks pass with no compositor regression
</success_criteria>

<output>
After completion, create `.planning/phases/04-overlay-system/04-05-SUMMARY.md`
</output>
