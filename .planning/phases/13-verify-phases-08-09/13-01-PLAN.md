---
phase: 13-verify-phases-08-09
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/08-presets-reliability/08-VERIFICATION.md
  - .planning/phases/13-verify-phases-08-09/13-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`.planning/phases/08-presets-reliability/08-VERIFICATION.md` exists and records evidence for Phase 08 ROADMAP success criteria"
    - "Phase 08 preset behavior (polished + overrides) is verified via tests and/or runtime evidence"
  artifacts:
    - path: ".planning/phases/08-presets-reliability/08-VERIFICATION.md"
      provides: "Phase 08 verification report with status + evidence"
      contains: "phase: 08-presets-reliability"
    - path: "packages/core/test/presets/resolve.test.ts"
      provides: "Ground-truth tests for built-in preset definitions + override behavior"
      contains: "polished preset has catppuccin"
  key_links:
    - from: ".planning/phases/08-presets-reliability/08-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "Observable Truths table mirrors Phase 08 success criteria"
      pattern: 'User can add "preset": "polished"'
---

<objective>
Create the missing Phase 08 verification report so milestone audit verification coverage passes.

Purpose: Close Phase 13 gap for Phase 08 (work done; verification artifact missing).
Output: `.planning/phases/08-presets-reliability/08-VERIFICATION.md` with concrete evidence and an accurate status.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-presets-reliability/08-01-SUMMARY.md
@.planning/phases/08-presets-reliability/08-02-SUMMARY.md
@.planning/phases/08-presets-reliability/08-03-SUMMARY.md
@.planning/phases/08-presets-reliability/08-04-SUMMARY.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md
@packages/core/test/presets/resolve.test.ts
@packages/cli/src/commands/init.ts
@packages/core/src/recorder.ts
@packages/core/src/compositor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect Phase 08 evidence (tests + minimal runtime)</name>
  <files>.planning/phases/08-presets-reliability/08-VERIFICATION.md</files>
  <action>
Collect evidence to support each Phase 08 ROADMAP success criterion.

1. Run baseline checks (paste representative lines into the report):
   - `pnpm test`
   - `pnpm build`

2. Use tests as primary evidence for preset semantics:
   - Cite `packages/core/test/presets/resolve.test.ts` to prove:
     - `polished` implies `theme: catppuccin`, sound enabled, cursor visible, HUD visible
     - user config overrides preset defaults (theme/sound/cursor/hud)

3. Minimal runtime evidence for "preset produces polished output":
   - Create a temporary config file in `/tmp/tuireel-verify-08-polished.jsonc` by copying the repo fixture `packages/core/test/fixtures/smoke.tuireel.jsonc` and adding:
     - `"preset": "polished"`
     - output path under `/tmp/` (e.g. `/tmp/tuireel-verify-08-polished.mp4`)
     - keep fps low (10) to keep runtime fast
   - Run: `node packages/cli/dist/index.js record /tmp/tuireel-verify-08-polished.jsonc`
   - If `ffprobe` is available, optionally include evidence that the output exists and includes an audio stream when preset sound is enabled.

4. For `tuireel init` preset prompt:
   - Prefer automated evidence if you can run it in a real TTY.
   - If not possible (non-TTY), mark this item under "Human Verification Required" and cite the code path in `packages/cli/src/commands/init.ts` (`process.stdin.isTTY` + `promptPreset()`).

5. For Ctrl+C cleanup and 1000+ frames:
   - If you can run reliable automated interrupts and long runs, include runtime evidence.
   - Otherwise, mark as "Human Verification Required" and use code anchors: - `packages/core/src/recorder.ts` signal handling + abort cleanup - `packages/core/src/compositor.ts` signal handling + temp directory usage
     </action>
     <verify>
     `pnpm test` and `pnpm build` succeed.

If `node packages/cli/dist/index.js record /tmp/tuireel-verify-08-polished.jsonc` fails due to missing external deps (e.g. `ffmpeg`, desktop capture), treat that as an acceptable outcome for this environment: capture the failure details in `08-VERIFICATION.md`, set `status: human_needed`, and list the exact manual steps + expected output to rerun the command on a properly provisioned machine.
</verify>
<done>Evidence exists for every Phase 08 success criterion (automated or explicitly marked human-needed); runtime-only items are allowed to be `human_needed` when external deps are missing, as long as the report includes concrete manual reproduction steps.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 08 verification report (numeric filename)</name>
  <files>.planning/phases/08-presets-reliability/08-VERIFICATION.md</files>
  <action>
Create `.planning/phases/08-presets-reliability/08-VERIFICATION.md` using the canonical template at `@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md`.

Requirements for the report:

- YAML frontmatter:
  - `phase: 08-presets-reliability`
  - `verified: <ISO timestamp>`
  - `status: passed | human_needed` (use the accurate status)
  - `score: N/M must-haves verified`
- Observable Truths table MUST mirror Phase 08 success criteria from `.planning/ROADMAP.md`.
- Include Required Artifacts and Key Link Verification sections; cite exact files and what proves they are substantive.
- If any truth cannot be verified non-interactively (init prompt, Ctrl+C cleanup, 1000+ frames), list it under "Human Verification Required" with concrete steps and expected outcomes.

Important: Use numeric report filename `08-VERIFICATION.md` (do NOT use `{phase-name}-VERIFICATION.md`).
</action>
<verify>
`git status --porcelain` shows the new file. The report frontmatter contains `phase: 08-presets-reliability` and a valid `status` value.
</verify>
<done>`08-VERIFICATION.md` exists, is substantive (not a stub), and provides evidence for all Phase 08 success criteria.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/08-presets-reliability/08-VERIFICATION.md`
- Ensure the report includes the 5 Phase 08 ROADMAP truths with evidence and an accurate status.
</verification>

<success_criteria>

- Phase 08 verification report exists at `.planning/phases/08-presets-reliability/08-VERIFICATION.md`.
- Report status is accurate (`passed` if fully verified; otherwise `human_needed` with explicit manual steps).
  </success_criteria>

<output>
After completion, create `.planning/phases/13-verify-phases-08-09/13-01-SUMMARY.md`
</output>
