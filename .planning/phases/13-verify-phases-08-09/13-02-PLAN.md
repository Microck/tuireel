---
phase: 13-verify-phases-08-09
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/09-diagnostics/09-VERIFICATION.md
  - .planning/phases/13-verify-phases-08-09/13-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`.planning/phases/09-diagnostics/09-VERIFICATION.md` exists and records evidence for Phase 09 ROADMAP success criteria"
    - "Verbose/debug record flags and actionable error guidance are verified via code anchors and runtime output"
  artifacts:
    - path: ".planning/phases/09-diagnostics/09-VERIFICATION.md"
      provides: "Phase 09 verification report with status + evidence"
      contains: "phase: 09-diagnostics"
    - path: "packages/cli/src/commands/record.ts"
      provides: "CLI flags --verbose/--debug mapped to logger levels"
      contains: "--verbose"
  key_links:
    - from: "packages/cli/src/commands/record.ts"
      to: "packages/core/src/logger.ts"
      via: "createLogger(logLevel) from CLI flags"
      pattern: "createLogger"
---

<objective>
Create the missing Phase 09 verification report so milestone audit verification coverage passes.

Purpose: Close Phase 13 gap for Phase 09 (work done; verification artifact missing).
Output: `.planning/phases/09-diagnostics/09-VERIFICATION.md` with concrete evidence and an accurate status.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-diagnostics/09-01-SUMMARY.md
@.planning/phases/09-diagnostics/09-02-SUMMARY.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md
@packages/cli/src/commands/record.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect Phase 09 runtime evidence (verbose/debug + actionable errors)</name>
  <files>.planning/phases/09-diagnostics/09-VERIFICATION.md</files>
  <action>
Collect evidence for each Phase 09 ROADMAP success criterion.

Note: verbose/debug logger output is emitted on stderr by design. When capturing output for the report, redirect stderr into stdout (use `2>&1`) so snippets include the actual log lines.

1. Baseline checks (cite in report):
   - `pnpm test`
   - `pnpm build`

2. Runtime evidence for `record --verbose` and `record --debug`:
   - Create `/tmp/tuireel-verify-09.jsonc` by copying `packages/core/test/fixtures/smoke.tuireel.jsonc` and setting output to `/tmp/tuireel-verify-09.mp4`.
   - Run and capture representative output lines:
     - `node packages/cli/dist/index.js record --verbose /tmp/tuireel-verify-09.jsonc 2>&1`
     - `node packages/cli/dist/index.js record --debug /tmp/tuireel-verify-09.jsonc 2>&1`
   - Ensure captured output demonstrates step-by-step progress and extra debug detail (timings / ffmpeg detail) as appropriate.

3. Actionable error guidance evidence:
   - Create an intentionally invalid config at `/tmp/tuireel-verify-09-invalid.jsonc` (e.g., empty steps or unknown preset/theme).
   - Run: `node packages/cli/dist/index.js validate /tmp/tuireel-verify-09-invalid.jsonc`
   - Capture error output that includes a concrete "Try:" suggestion.

4. Code anchor evidence:
   - Cite `packages/cli/src/commands/record.ts` for flag->LogLevel mapping (`--verbose`, `--debug`).
   - Cite a small number of representative `Try:` errors in core (grep for `Try:` if helpful) to demonstrate systemic coverage.
     </action>
     <verify>
     `pnpm test` and `pnpm build` succeed.

If `node packages/cli/dist/index.js record --verbose /tmp/tuireel-verify-09.jsonc 2>&1` or `--debug` fails due to missing external deps (e.g. `ffmpeg`, desktop capture), treat that as an acceptable outcome for this environment: capture the failure details in `09-VERIFICATION.md`, set `status: human_needed`, and list the exact manual steps + expected output to rerun the commands on a properly provisioned machine.

`node packages/cli/dist/index.js validate /tmp/tuireel-verify-09-invalid.jsonc` prints an actionable suggestion (a "Try:" style hint), OR is explicitly marked `human_needed` with concrete reproduction steps.
</verify>
<done>Collected CLI output snippets and code anchors that prove all three Phase 09 success criteria (or explicitly marked human-needed when runtime deps are missing, with concrete manual reproduction steps in the report).</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 09 verification report (numeric filename)</name>
  <files>.planning/phases/09-diagnostics/09-VERIFICATION.md</files>
  <action>
Create `.planning/phases/09-diagnostics/09-VERIFICATION.md` using the canonical template at `@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md`.

Requirements for the report:

- YAML frontmatter:
  - `phase: 09-diagnostics`
  - `verified: <ISO timestamp>`
  - `status: passed | human_needed` (use `passed` only if all truths are verified via captured evidence; otherwise `human_needed` with explicit steps)
  - `score: N/M must-haves verified`
- Observable Truths table MUST mirror Phase 09 success criteria from `.planning/ROADMAP.md`.
- Include runtime evidence snippets for `--verbose` and `--debug` (remember output is on stderr; capture with `2>&1`) and at least one "Try:" style actionable error.
- If any truth cannot be verified non-interactively in this environment, list it under "Human Verification Required" with concrete steps and expected outcomes.

Important: Use numeric report filename `09-VERIFICATION.md` (do NOT use `{phase-name}-VERIFICATION.md`).
</action>
<verify>
`test -f .planning/phases/09-diagnostics/09-VERIFICATION.md` and the report frontmatter contains `phase: 09-diagnostics` and `status: passed` OR `status: human_needed`.
</verify>
<done>`09-VERIFICATION.md` exists, is substantive, and provides evidence for all Phase 09 success criteria.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/09-diagnostics/09-VERIFICATION.md`
- Ensure the report includes the 3 Phase 09 ROADMAP truths with evidence and an accurate `status` (`passed` only when fully verified).
</verification>

<success_criteria>

- Phase 09 verification report exists at `.planning/phases/09-diagnostics/09-VERIFICATION.md`.
- Report maps directly to ROADMAP success criteria and includes runtime output snippets.
  </success_criteria>

<output>
After completion, create `.planning/phases/13-verify-phases-08-09/13-02-SUMMARY.md`
</output>
