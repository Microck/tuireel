---
phase: 26-human-pacing-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/executor/pacing/profiles.ts
  - packages/core/src/executor/pacing/index.ts
  - packages/core/src/executor/timing.ts
  - packages/core/src/config/schema.ts
  - packages/core/test/pacing/profiles.test.ts
  - packages/core/test/timing.test.ts
  - packages/core/test/config.test.ts
autonomous: true
requirements: [PACE-01]

must_haves:
  truths:
    - "Named cadence profiles (relaxed, brisk, deliberate) exist and resolve to deterministic timing parameters"
    - "charDelay() accepts optional profile and uses profile multipliers instead of hardcoded values"
    - "Config schema accepts a `pacing` field with profile name or inline object"
    - "No pacing field = identical behavior to current code (backward compatible)"
  artifacts:
    - path: "packages/core/src/executor/pacing/profiles.ts"
      provides: "CadenceProfile type and built-in profiles"
      exports: ["CadenceProfile", "CADENCE_PROFILES", "CADENCE_PROFILE_NAMES", "resolveProfile"]
    - path: "packages/core/src/executor/pacing/index.ts"
      provides: "Public barrel export"
      exports: ["CadenceProfile", "CADENCE_PROFILES", "CADENCE_PROFILE_NAMES", "resolveProfile"]
    - path: "packages/core/src/executor/timing.ts"
      provides: "Profile-aware charDelay"
      exports: ["charDelay", "sleep"]
    - path: "packages/core/src/config/schema.ts"
      provides: "pacing field in config schema"
      contains: "pacing"
    - path: "packages/core/test/pacing/profiles.test.ts"
      provides: "Profile resolution and charDelay profile tests"
      min_lines: 30
  key_links:
    - from: "packages/core/src/config/schema.ts"
      to: "packages/core/src/executor/pacing/profiles.ts"
      via: "CADENCE_PROFILE_NAMES import for schema enum"
      pattern: "CADENCE_PROFILE_NAMES"
    - from: "packages/core/src/executor/timing.ts"
      to: "packages/core/src/executor/pacing/profiles.ts"
      via: "CadenceProfile type import for charDelay signature"
      pattern: "CadenceProfile"
---

<objective>
Define the cadence profile data model, extend `charDelay()` to accept profile-driven multipliers, and add a `pacing` field to the config schema so authors can select a named profile.

Purpose: PACE-01 requires authors to apply a deterministic cadence profile for human-feeling typing without hand-tuning. This plan creates the profile type, built-in profiles, profile-aware timing, and schema validation.
Output: `CadenceProfile` type + 3 built-in profiles, backward-compatible `charDelay()`, `pacing` field in config schema, all with tests.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-human-pacing-engine/26-CONTEXT.md
@.planning/phases/26-human-pacing-engine/26-RESEARCH.md

@packages/core/src/executor/timing.ts
@packages/core/src/config/schema.ts
@packages/core/test/timing.test.ts
@packages/core/test/config.test.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/core/src/executor/timing.ts:

```typescript
export function charDelay(
  baseMs: number,
  context?: { text: string; char: string; index: number },
): number;

export function sleep(ms: number): Promise<void>;
```

From packages/core/src/config/schema.ts:

```typescript
const baseConfigFields = {
  // ... existing fields including fps, captureFps, cols, rows, etc.
} satisfies Record<string, z.ZodTypeAny>;

export const configSchema = z.object({ ...baseConfigFields, steps: z.array(stepSchema).min(1) });
export const singleVideoInputConfigSchema = z.object({
  ...baseConfigFields,
  steps: stepArraySchema,
});
export const videoDefinitionSchema = z.object({
  /* mirrors baseConfigFields as optional + steps */
});
export type TuireelConfig = z.infer<typeof configSchema>;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create cadence profile types, built-in profiles, and profile tests</name>
  <files>packages/core/src/executor/pacing/profiles.ts, packages/core/src/executor/pacing/index.ts, packages/core/test/pacing/profiles.test.ts</files>
  <behavior>
    - Test: resolveProfile("relaxed") returns relaxed profile with expected baseSpeedMs
    - Test: resolveProfile("brisk") returns brisk profile with expected baseSpeedMs
    - Test: resolveProfile("deliberate") returns deliberate profile with expected baseSpeedMs
    - Test: resolveProfile(undefined) returns undefined (no profile = backward compat)
    - Test: resolveProfile(inlineObject) returns the inline object as-is
    - Test: all built-in profiles have positive baseSpeedMs and all beat durations > 0
    - Test: CADENCE_PROFILE_NAMES contains exactly ["relaxed", "brisk", "deliberate"]
  </behavior>
  <action>
Create `packages/core/src/executor/pacing/profiles.ts`:
- Define `CadenceProfile` interface with fields: `baseSpeedMs`, `firstCharExtra`, `punctuationExtra`, `whitespaceExtra`, `pathSepExtra`, `beats: { startup, settle, read, idle }` (all numbers).
- Define `CADENCE_PROFILES` as a `Record<string, CadenceProfile>` with three built-in profiles:
  - `relaxed`: baseSpeedMs ~65, generous beats (~800/500/400/250)
  - `brisk`: baseSpeedMs ~38, shorter beats (~400/250/200/120)
  - `deliberate`: baseSpeedMs ~90, longer beats (~1200/700/600/350)
- Export `CADENCE_PROFILE_NAMES` as the profile name tuple derived from the keys.
- Export `resolveProfile(input: string | CadenceProfile | undefined): CadenceProfile | undefined` that looks up by name or passes through an inline profile.

Create `packages/core/src/executor/pacing/index.ts`: re-export everything from `profiles.ts`.

Create tests first in `packages/core/test/pacing/profiles.test.ts`, run RED, then implement GREEN.
</action>
<verify>
<automated>pnpm --filter @tuireel/core vitest run test/pacing/profiles.test.ts -x</automated>
</verify>
<done>CadenceProfile type exported, 3 built-in profiles exist with valid timing parameters, resolveProfile handles string names, inline objects, and undefined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend charDelay with profile support and add pacing to config schema</name>
  <files>packages/core/src/executor/timing.ts, packages/core/src/config/schema.ts, packages/core/test/timing.test.ts, packages/core/test/config.test.ts</files>
  <behavior>
    - Test: charDelay with no profile produces identical results to current behavior (exact same values as before)
    - Test: charDelay with relaxed profile uses profile's firstCharExtra instead of hardcoded 0.24
    - Test: charDelay with brisk profile produces shorter delays than relaxed for same input
    - Test: charDelay determinism preserved with profile parameter
    - Test: config with `"pacing": "relaxed"` validates successfully
    - Test: config with `"pacing": "invalid-name"` fails validation
    - Test: config without pacing field validates successfully (backward compat)
    - Test: generated JSON schema includes pacing field
  </behavior>
  <action>
Extend `charDelay()` in `packages/core/src/executor/timing.ts`:
- Add optional third parameter `profile?: CadenceProfile` (import from `./pacing/profiles.js`).
- When profile is provided, use `profile.firstCharExtra` instead of hardcoded `0.24`, `profile.pathSepExtra` instead of `0.08`, `profile.whitespaceExtra` instead of `0.28`, `profile.punctuationExtra` instead of `0.22`, and `profile.pathSepExtra` instead of `0.06` for previous-char path sep.
- When profile is undefined, keep exact current hardcoded values. This ensures backward compatibility.

Extend `packages/core/src/config/schema.ts`:

- Import `CADENCE_PROFILE_NAMES` from `../executor/pacing/profiles.js`.
- Create `pacingSchema` as `z.union([z.enum(CADENCE_PROFILE_NAMES), z.object({ baseSpeedMs: z.number().positive(), firstCharExtra: z.number().nonnegative(), punctuationExtra: z.number().nonnegative(), whitespaceExtra: z.number().nonnegative(), pathSepExtra: z.number().nonnegative(), beats: z.object({ startup: z.number().nonnegative(), settle: z.number().nonnegative(), read: z.number().nonnegative(), idle: z.number().nonnegative() }) })]).optional()`.
- Add `pacing: pacingSchema` to `baseConfigFields`.
- Add `pacing` field (optional) to `videoDefinitionSchema`.

Extend existing tests:

- In `test/timing.test.ts`: Add tests for profile-aware charDelay (backward compat + profile-driven values).
- In `test/config.test.ts`: Add tests for pacing field validation and JSON schema generation including pacing.

Write tests first (RED), then implement (GREEN).
</action>
<verify>
<automated>pnpm --filter @tuireel/core vitest run test/timing.test.ts test/config.test.ts -x</automated>
</verify>
<done>charDelay accepts optional profile, uses profile multipliers when present, falls back to hardcoded values when absent. Config schema validates pacing field. JSON schema includes pacing. All existing tests still pass.</done>
</task>

</tasks>

<verification>
pnpm --filter @tuireel/core vitest run -x
</verification>

<success_criteria>

- `CadenceProfile` type and 3 built-in profiles exported from `packages/core/src/executor/pacing/`
- `charDelay()` accepts optional profile parameter; backward compatible when omitted
- Config schema accepts `pacing` field (named string or inline object)
- Generated JSON schema includes pacing
- All existing timing, config, and type-step tests still pass
- New profile and timing tests pass
  </success_criteria>

<output>
After completion, create `.planning/phases/26-human-pacing-engine/26-01-SUMMARY.md`
</output>
