---
phase: 17-fix-multi-format-record-outputs
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - docs/cli-reference.mdx
autonomous: true

must_haves:
  truths:
    - "Docs describe the actual `--format` behavior, including output filename normalization"
    - "Docs do not imply that `--format` only changes codecs while keeping output extension unchanged"
  artifacts:
    - path: "docs/cli-reference.mdx"
      provides: "CLI command reference including record/composite --format behavior"
      contains: "--format"
  key_links:
    - from: "docs/cli-reference.mdx"
      to: "tuireel record"
      via: "record section text"
      pattern: "Record a TUI session"
---

<objective>
Update documentation so `--format` behavior matches runtime: selecting a format also normalizes the output filename extension.

Purpose: Prevent docs/runtime mismatch and make CI-04 expectations obvious to users.
Output: Updated `docs/cli-reference.mdx` with a clear note and example.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-fix-multi-format-record-outputs/17-RESEARCH.md

@docs/cli-reference.mdx
@.github/workflows/video-smoke.yml
@packages/core/test/fixtures/smoke.tuireel.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document record --format output filename normalization (with rule + example)</name>
  <files>docs/cli-reference.mdx</files>
  <action>
In the `## record` section, keep the flag table, but add a short note explaining the output naming contract:

- `--format` is authoritative for container/codec selection.
- When a format is selected (via `--format` or config `format`), Tuireel normalizes the output path to end with `.<format>`.
- Normalization rule: append extension if missing; otherwise replace the last extension.

Add a concrete example that matches CI smoke:

- If config output is `smoke-output.mp4`, then `tuireel record --format webm` writes `smoke-output.webm`.

Keep wording concise and user-facing (no internal file references).
</action>
<verify>
rg -n "normaliz|normalize" docs/cli-reference.mdx
rg -n "smoke-output\\.webm" docs/cli-reference.mdx
</verify>
<done>Docs explicitly state that selecting a format rewrites the output extension, with an example.</done>
</task>

<task type="auto">
  <name>Task 2: Align composite --format docs with the same naming rule</name>
  <files>docs/cli-reference.mdx</files>
  <action>
In the `## composite` section, add a short sentence (or bullet) confirming that `--format` also normalizes the output filename extension for the composited output.

Goal: users do not assume composite will write GIF/WebM bytes into a `.mp4` path.
</action>
<verify>
rg -n "## composite" -n docs/cli-reference.mdx
rg -n "--format" -n docs/cli-reference.mdx
</verify>
<done>`docs/cli-reference.mdx` describes consistent output naming for both record and composite when a format is selected.</done>
</task>

</tasks>

<verification>
- `docs/cli-reference.mdx` record and composite sections describe the same filename normalization behavior.
- There is no longer any implied behavior of "codec changes but filename stays .mp4".
</verification>

<success_criteria>

- Roadmap truth (3): Docs accurately describe `--format` behavior.
  </success_criteria>

<output>
After completion, create `.planning/phases/17-fix-multi-format-record-outputs/17-03-SUMMARY.md`
</output>
