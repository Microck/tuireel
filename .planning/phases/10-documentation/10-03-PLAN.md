---
phase: 10-documentation
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - docs/themes.mdx
  - docs/presets.mdx
  - docs/sound.mdx
  - docs/multi-video.mdx
autonomous: true

must_haves:
  truths:
    - "Themes page documents all built-in themes and custom theme configuration"
    - "Presets page documents all built-in presets with usage examples"
    - "Sound page documents sound effects and custom audio track configuration"
    - "Multi-video page documents multi-video configs and shared step includes"
  artifacts:
    - path: "docs/themes.mdx"
      provides: "Theme documentation with built-in list and custom theme guide"
    - path: "docs/presets.mdx"
      provides: "Preset documentation with all 4 built-in presets"
    - path: "docs/sound.mdx"
      provides: "Sound configuration documentation"
    - path: "docs/multi-video.mdx"
      provides: "Multi-video and includes documentation"
  key_links:
    - from: "docs/presets.mdx"
      to: "packages/core/src/presets/built-in.ts"
      via: "Documents each preset's defaults"
      pattern: "polished|minimal|demo|silent"
---

<objective>
Create feature documentation pages for themes, presets, sound, and multi-video workflows.

Purpose: Users can understand and configure all Tuireel features with examples.
Output: Four feature pages completing the documentation site.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/core/src/themes/built-in.ts
@packages/core/src/themes/schema.ts
@packages/core/src/presets/built-in.ts
@packages/core/src/sound.ts
@packages/core/src/config/loader.ts
@packages/core/src/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Themes and Presets pages</name>
  <files>docs/themes.mdx, docs/presets.mdx</files>
  <action>
**Themes page (`docs/themes.mdx`):**
Read `packages/core/src/themes/built-in.ts` for the exact list and `themes/schema.ts` for the ThemeConfig shape. Content:

- Title: "Themes"
- Built-in themes section: list each (dracula, catppuccin, one-dark, solarized-dark, tokyo-night, gruvbox-dark) with their color previews (use the actual hex values from built-in.ts)
- Usage: `"theme": "dracula"` in config
- Custom themes: show the ThemeConfig object shape with all color fields (background, foreground, cursor, plus 16 ANSI colors), font settings
- Example: creating a custom theme inline in config
- Example: referencing a theme file path

**Presets page (`docs/presets.mdx`):**
Read `packages/core/src/presets/built-in.ts` for exact definitions. Content:

- Title: "Presets"
- What are presets: named bundles of presentation defaults (theme, sound, cursor, hud)
- How they work: preset values are defaults, user config overrides them
- Built-in presets table:
  - `polished`: catppuccin + sound (click+key variant 1) + cursor + HUD
  - `minimal`: tokyo-night + cursor only
  - `demo`: dracula + sound (click+key variant 1) + cursor + HUD
  - `silent`: no cursor, no HUD, no sound
- Usage: `"preset": "polished"` in config
- Override example: preset sets sound on, user turns it off
- Init integration: `tuireel init` prompts for preset selection

Use `<CodeGroup>` for alternative config examples where appropriate.
  </action>
  <verify>
`ls docs/themes.mdx docs/presets.mdx` both exist. All built-in themes and presets from the source code are documented.
  </verify>
  <done>Themes page lists all built-in themes with colors and shows custom theme creation. Presets page documents all 4 presets with their defaults and override behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Sound and Multi-video pages</name>
  <files>docs/sound.mdx, docs/multi-video.mdx</files>
  <action>
**Sound page (`docs/sound.mdx`):**
Read `packages/core/src/sound.ts` and the sound schema from config/schema.ts. Content:

- Title: "Sound"
- Overview: sound is opt-in; configured via the `sound` config field or included in presets
- Sound effects: `effects.click` and `effects.key` — each accepts a numbered variant (1-4) or a custom audio file path
- Custom audio track: `sound.track` (path to audio file, relative to config), `sound.trackVolume` (0-1, default 0.3)
- Effects volume: `sound.effectsVolume` (0-1, default 0.5)
- Example: enabling sound with built-in effects
- Example: using a custom background track
- Example: using a preset that includes sound (polished, demo)
- Note: sound is synced to step actions (clicks and keypresses)

**Multi-video page (`docs/multi-video.mdx`):**
Read `packages/core/src/config/loader.ts` to understand how multi-video and includes work. Content:

- Title: "Multi-video & Includes"
- Multi-video configs: a single .tuireel.jsonc can define an array of configs (top-level array instead of object)
- Each video in the array gets its own output file and settings
- Shared step includes: reference external JSONC files for reusable step sequences
- Usage: `"steps": [{ "$include": "./shared/setup.jsonc" }, { "type": "type", "text": "hello" }]`
- Example: multi-video config with shared setup steps
- Example: extracting common steps into a shared include file
- When to use: demo suites, multiple output formats, shared onboarding sequences

**Read the actual source files for exact behavior. Do NOT guess include syntax or multi-video format.**
  </action>
  <verify>
`ls docs/sound.mdx docs/multi-video.mdx` both exist. Sound page covers effects, tracks, and volume. Multi-video page covers array configs and includes.
  </verify>
  <done>Sound page documents all sound configuration with examples. Multi-video page documents array configs and shared step includes.</done>
</task>

</tasks>

<verification>
- docs/themes.mdx lists all 5+ built-in themes
- docs/presets.mdx documents all 4 presets with exact defaults
- docs/sound.mdx covers effects, custom tracks, and volume
- docs/multi-video.mdx covers array configs and includes
- All pages have valid MDX frontmatter
</verification>

<success_criteria>
DOCS-07, DOCS-08, DOCS-09, DOCS-10 are implemented: themes, presets, sound, and multi-video pages exist with comprehensive examples.
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-03-SUMMARY.md`
</output>
