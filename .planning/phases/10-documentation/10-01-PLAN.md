---
phase: 10-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/docs.json
  - docs/introduction.mdx
  - docs/quickstart.mdx
  - docs/package.json
  - pnpm-workspace.yaml
autonomous: true

must_haves:
  truths:
    - "Running `npx mintlify dev` in docs/ serves a docs site with logo, colors, and navigation"
    - "A new user can follow the Quickstart page to install tuireel and produce their first recording"
    - "Introduction page explains what Tuireel is and why it exists"
  artifacts:
    - path: "docs/docs.json"
      provides: "Mintlify configuration with branding, nav, and theme"
      contains: "tuireel"
    - path: "docs/introduction.mdx"
      provides: "Landing page for documentation"
    - path: "docs/quickstart.mdx"
      provides: "Step-by-step first recording guide"
  key_links:
    - from: "docs/docs.json"
      to: "assets/branding/"
      via: "logo and favicon references"
      pattern: "logo|favicon"
---

<objective>
Initialize the Mintlify docs site with branding, navigation, introduction, and quickstart pages.

Purpose: Establish the documentation foundation that all reference pages build on.
Output: Working Mintlify site with intro and quickstart, branded with Tuireel colors and logo.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@assets/branding/
@packages/cli/src/index.ts
@packages/core/src/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Mintlify docs site with branding and navigation</name>
  <files>docs/docs.json, docs/package.json, pnpm-workspace.yaml</files>
  <action>
Create `docs/` directory as a Mintlify docs project:

1. Create `docs/docs.json` (Mintlify v4 config format):
   - `name`: "Tuireel"
   - `logo`: dark and light variants pointing to `../assets/branding/logo-dark.svg` and `logo-light.svg` (or copy into docs if Mintlify needs local paths — check Mintlify convention)
   - `favicon`: reference to `../assets/branding/favicon.svg`
   - `colors.primary`: "#06B6D4" (teal), `colors.light`: "#06B6D4", `colors.dark`: "#0F172A" (navy)
   - `navigation`: structured nav with groups:
     - "Get Started": ["introduction", "quickstart"]
     - "Configuration": ["config-reference", "steps-reference", "cli-reference"]
     - "Features": ["themes", "presets", "sound", "multi-video"]
   - `topbarLinks`: GitHub repo link
   - `anchors`: GitHub repo anchor
   - `footerSocials`: GitHub link

2. Create `docs/package.json` with `mintlify` as devDependency for local dev:
   ```json
   { "name": "tuireel-docs", "private": true, "scripts": { "dev": "mintlify dev" }, "devDependencies": { "mintlify": "latest" } }
   ```

3. Add `"docs"` to `pnpm-workspace.yaml` packages array if not present.

4. Copy branding assets into `docs/` if Mintlify requires local paths (logo SVGs, favicon). Use `docs/images/` subdirectory.

**Important:** Use Mintlify v4 `docs.json` format (not the older `mint.json`). Check the Mintlify docs if unsure about the exact field names.
  </action>
  <verify>
`ls docs/docs.json` exists. `cat docs/docs.json | node -e "JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'))"` parses valid JSON. Logo and favicon paths reference existing files.
  </verify>
  <done>Mintlify docs site is initialized with Tuireel branding (teal/navy colors, logo, favicon) and navigation structure covering all planned pages.</done>
</task>

<task type="auto">
  <name>Task 2: Create Introduction and Quickstart pages</name>
  <files>docs/introduction.mdx, docs/quickstart.mdx</files>
  <action>
**Introduction page (`docs/introduction.mdx`):**
- Title: "Introduction"
- Description: "Scripted TUI demo recordings — no manual editing required"
- Content sections:
  - What is Tuireel? (2-3 paragraphs: declarative JSONC config, virtual PTY capture, polished video output)
  - Why Tuireel? (bullet list: repeatable, scripted, polished overlays, multiple formats)
  - How it works (brief pipeline: script → record → composite → video)
  - Quick example showing a minimal .tuireel.jsonc config
- Use Mintlify components: `<Card>`, `<CardGroup>` for feature highlights
- Link to Quickstart as the primary CTA

**Quickstart page (`docs/quickstart.mdx`):**
- Title: "Quickstart"
- Description: "Install Tuireel and produce your first recording in under 5 minutes"
- Steps:
  1. Install: `npm install -g tuireel` or `npx tuireel`
  2. Init: `tuireel init` — creates a .tuireel.jsonc scaffold
  3. Validate: `tuireel validate .tuireel.jsonc`
  4. Record: `tuireel record .tuireel.jsonc`
  5. Output: view the generated output.mp4
- Use Mintlify `<Steps>` component for numbered steps
- Use `<CodeGroup>` for npm vs bun install alternatives
- Include a sample .tuireel.jsonc with a simple `launch` + `type` + `press` flow
- Mention presets: add `"preset": "polished"` for instant polish

Both pages should use frontmatter: `title`, `description`, `icon` (Mintlify icon name).
  </action>
  <verify>
`ls docs/introduction.mdx docs/quickstart.mdx` both exist. Files contain valid MDX with Mintlify frontmatter.
  </verify>
  <done>Introduction explains Tuireel's purpose and value. Quickstart guides a new user from install to first recording in 5 steps.</done>
</task>

</tasks>

<verification>
- `docs/docs.json` is valid JSON with logo, colors, favicon, and navigation
- `docs/introduction.mdx` exists with proper frontmatter and content
- `docs/quickstart.mdx` exists with install-to-record flow
- Navigation in docs.json references all created pages
</verification>

<success_criteria>
DOCS-01, DOCS-02, DOCS-03 are implemented: Mintlify site initialized with branding, intro page explains the project, quickstart guides first recording.
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation/10-01-SUMMARY.md`
</output>
