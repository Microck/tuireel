---
phase: 16-publish-installability-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/package.json
  - pnpm-lock.yaml
  - packages/cli/src/index.ts
  - packages/core/src/index.ts
autonomous: true

must_haves:
  truths:
    - "tuireel's published dependency graph contains no workspace:* ranges"
    - "tuireel --help / --version does not rely on a stale hard-coded VERSION"
    - "@tuireel/core does not export an incorrect hard-coded VERSION string"
  artifacts:
    - path: "packages/cli/package.json"
      provides: "Publishable dependency range for @tuireel/core"
      contains: '"@tuireel/core"'
    - path: "packages/cli/src/index.ts"
      provides: "Commander version sourced from CLI package version"
      contains: "program.version"
    - path: "packages/core/src/index.ts"
      provides: "Core public exports without stale VERSION drift"
  key_links:
    - from: "packages/cli/package.json"
      to: "@tuireel/core"
      via: "dependencies range"
      pattern: "\\\"@tuireel/core\\\"\\s*:\\s*\\\"(?!workspace:)[^\\\"]+\\\""
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/package.json"
      via: "createRequire -> require(../package.json)"
      pattern: "createRequire\\(import\\.meta\\.url\\)"
---

<objective>
Remove publish-breaking `workspace:*` dependency ranges and eliminate version-string drift.

Purpose: Ensure `npx tuireel --help` can run from a clean install and version reporting matches the published artifact.
Output: Updated package metadata and runtime version wiring for `tuireel` and `@tuireel/core`.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-publish-installability-fixes/16-RESEARCH.md

@packages/cli/package.json
@packages/cli/src/index.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CLI dependency on core with real semver</name>
  <files>
  packages/cli/package.json
  pnpm-lock.yaml
  </files>
  <action>
Update `dependencies["@tuireel/core"]` from `workspace:*` to a publishable semver range.

- Use an exact version string matching the current fixed-group version (currently `0.1.0`).
- Do not introduce any `workspace:` ranges anywhere in publishable package.json files.
- Expect `pnpm-lock.yaml` to update after this change; keep the lockfile consistent with the new semver range.

Mitigation note (local workspace installs):

- pnpm should still link local workspace packages when the semver range is satisfied.
- If pnpm attempts to fetch `@tuireel/core` from the registry during local workspace installs, record this as a critical gap and propose a follow-up plan to add repo-local config (e.g. `.npmrc` with `link-workspace-packages=true`). Do not add that config in Phase 16 unless it is required to make installs work.
  </action>
  <verify>
  pnpm -w install
  pnpm -w install --frozen-lockfile
  </verify>
  <done>`packages/cli/package.json` contains `"@tuireel/core": "0.1.0"` (or equivalent non-workspace semver) and workspace install still succeeds.</done>
  </task>

<task type="auto">
  <name>Task 2: Source CLI version from its own package.json</name>
  <files>packages/cli/src/index.ts</files>
  <action>
Stop importing `VERSION` from `@tuireel/core` and make commander version derive from `tuireel`'s own published version.

- Use `createRequire(import.meta.url)` and `require("../package.json")` (ESM-safe) to read `{ version }`.
- Set `program.version(version)`.
- Keep the rest of CLI wiring unchanged.
  </action>
  <verify>
  pnpm -w build
  </verify>
  <done>`packages/cli/src/index.ts` no longer imports `VERSION` from `@tuireel/core`, and `pnpm -w build` succeeds.</done>
  </task>

<task type="auto">
  <name>Task 3: Remove or de-drift core VERSION export</name>
  <files>packages/core/src/index.ts</files>
  <action>
Ensure `@tuireel/core` does not export a stale hard-coded version string.

Preferred (simplest): remove `export const VERSION = ...` entirely.

If removal causes a compile/test failure elsewhere, keep the export but derive it from `packages/core/package.json` at runtime (no hard-coded literal), using the same `createRequire(import.meta.url)` approach.
</action>
<verify>
pnpm -w test
</verify>
<done>There is no exported hard-coded `VERSION = "..."` literal in `packages/core/src/index.ts`, and the workspace test suite passes.</done>
</task>

</tasks>

<verification>
- Confirm no `workspace:` strings remain in publishable package.json files: `rg "workspace:" packages -g"package.json"`
- Verify CLI help works from built dist: `node packages/cli/dist/index.js --help`
- Verify CLI version works from built dist: `node packages/cli/dist/index.js --version`
</verification>

<success_criteria>

- `packages/cli/package.json` declares `@tuireel/core` with a non-workspace semver range
- CLI help/version works after build in repo (`node packages/cli/dist/index.js --help` and `node packages/cli/dist/index.js --version`)
- Core no longer exports an incorrect hard-coded `VERSION` literal
  </success_criteria>

<output>
After completion, create `.planning/phases/16-publish-installability-fixes/16-01-SUMMARY.md`
</output>
