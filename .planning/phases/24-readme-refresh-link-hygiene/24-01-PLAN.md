---
phase: 24-readme-refresh-link-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "README structure follows Webreel pattern: logo → tagline → badges → one-liner + docs link → Quick Start → Usage (CLI reference) → step types table → config table → Development → Packages → License"
    - "Every CLI command, flag, and step-type claim in the README matches docs/cli-reference.mdx and docs/steps-reference.mdx"
    - "README links to https://tuireel.micr.dev for documentation (no tuireel.dev references)"
    - "No stale docs-domain references exist in any user-facing file in the repo (README.md, CONTRIBUTING.md, docs/*.mdx, docs/docs.json, package.json files)"
    - "The brand-system link is removed from the user-facing README header"
  artifacts:
    - path: "README.md"
      provides: "Restructured README matching Webreel pattern with accurate Tuireel content"
      contains: "tuireel.micr.dev"
  key_links:
    - from: "README.md"
      to: "https://tuireel.micr.dev"
      via: "docs link in opener section"
      pattern: "tuireel\\.micr\\.dev"
    - from: "README.md"
      to: "docs/cli-reference.mdx"
      via: "CLI commands and flags match between README and docs"
      pattern: "tuireel (init|validate|record|preview|composite)"
---

<objective>
Rewrite the Tuireel README to match Webreel's structure and tone, fix all stale docs-domain links, and verify link hygiene across the repo.

Purpose: The README is the repo's front door. Matching the Webreel structure makes it scannable, self-contained, and consistent with the parent project's documentation style. Fixing stale links ensures users land on the correct docs domain.
Output: A fully restructured README.md and confirmed zero stale docs-domain references in user-facing files.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-readme-refresh-link-hygiene/24-RESEARCH.md
@README.md
@CONTRIBUTING.md
@docs/cli-reference.mdx
@docs/steps-reference.mdx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README to Webreel structure with accurate Tuireel content</name>
  <files>README.md</files>
  <action>
Rewrite README.md following the Webreel-adapted target structure from 24-RESEARCH.md. Use docs/cli-reference.mdx and docs/steps-reference.mdx as the source of truth for all CLI claims.

**Section-by-section spec (in order):**

1. **Logo block** — Keep the existing `<picture>` tag with dark/light logo variants. No changes needed.

2. **Banner** — Keep the existing banner `<img>` tag. No changes needed.

3. **Tagline** — Keep: "Scripted TUI demo recorder — produce polished terminal demo videos from declarative configs."

4. **Badges** — Keep the existing 3 badges (npm version, CI, license). Keep as-is.

5. **Horizontal rule** (`---`)

6. **One-liner + links** — Replace the current `## Install` opener with a terse description paragraph and an inline links line:

   ```
   Tuireel executes scripted terminal interactions in a virtual PTY, captures frames, and renders polished videos (MP4, WebM, GIF) with optional overlays and sound effects — no manual recording or editing required.

   [Documentation](https://tuireel.micr.dev) · [GitHub](https://github.com/Microck/tuireel)
   ```

7. **## Quick Start** — Three-line code block matching Webreel's brevity:

   ```bash
   npm install -g tuireel   # or: npx tuireel / bunx tuireel
   tuireel init              # scaffold a demo config
   tuireel record            # record your first demo
   ```

   Follow with a brief JSONC example showing a minimal config. Use CORRECT field names from the schema — `"text"` NOT `"value"`:

   ```jsonc
   {
     "output": "demo.mp4",
     "steps": [
       { "type": "type", "text": "echo 'Hello, world!'" },
       { "type": "press", "key": "enter" },
       { "type": "pause", "duration": 1000 },
     ],
   }
   ```

   NOTE: Step type `"enter"` does not exist per steps-reference.mdx. The correct way is `{ "type": "press", "key": "enter" }`. Fix the current README bug here.
   Also: remove trailing commas from JSONC arrays/objects to keep the example clean.

8. **## Usage** — Condensed CLI reference. For each subcommand, show the command + key flags in a compact format. Do NOT duplicate the full docs — give enough to be useful and link to docs for the complete reference.

   Structure:

   ```
   ### `tuireel init`
   Scaffold a new demo config.
   - `-o, --output <path>` — output file path
   - `-f, --force` — overwrite existing config

   ### `tuireel record`
   Record a demo from config.
   - `--format <mp4|webm|gif>` — output format (default: mp4)
   - `-w, --watch` — re-record on config change
   - `--verbose` / `--debug` — increase log output

   ### `tuireel preview`
   Preview a config without recording.
   - `--verbose` / `--debug`

   ### `tuireel composite`
   Re-render overlays on an existing recording.
   - `-c, --config <path>` — config path
   - `--format <mp4|webm|gif>` — output format
   - `--cursor-size <n>` — cursor size in pixels
   - `--no-cursor` / `--no-hud` — disable overlays
   - `--verbose` / `--debug`

   ### `tuireel validate`
   Validate a config file without recording.
   ```

   After the subcommands, add:

   **### Step Types** — table of all step types from docs/steps-reference.mdx:
   | Type | Key Fields | Description |
   With one row per step type: launch, type, press, wait, pause, scroll, click, screenshot, resize, set-env.

   **### Config Options** — condensed table of top-level config fields. Include: `output`, `format`, `theme`, `steps`, `fontSize`, `cols`, `rows`, `sound`, `cursor`, `hud`, `videos` (multi-video). Keep it brief — link to full docs.

   End the Usage section with: `> See the [full documentation](https://tuireel.micr.dev) for complete configuration reference.`

9. **## Development** — Pull from CONTRIBUTING.md:

   ````
   ### Prerequisites
   - Node.js 18+
   - pnpm 10+ (`corepack enable`)
   - ffmpeg on PATH

   ### Setup
   ```bash
   git clone https://github.com/Microck/tuireel.git
   cd tuireel
   pnpm install
   pnpm build
   ````

   ```

   ```

10. **## Packages** — Monorepo table:
    | Package | Description |
    |---------|-------------|
    | [`tuireel`](packages/cli) | CLI interface |
    | [`@tuireel/core`](packages/core) | Recording, compositing, and workflow engine |

11. **## License** — Single line: `[Apache 2.0](./LICENSE)`

**What to REMOVE from the current README:**

- Line 10: `Brand system: assets/branding/brand-system.md` link (internal, not user-facing)
- `## Features` section (replaced by inline CLI reference and description paragraph)
- `## Documentation` heading (replaced by inline link in opener)
- `## Contributing` heading (replaced by `## Development` section with setup info)
- The buggy JSONC example with `"value"` instead of `"text"` and `"enter"` step type

**What NOT to do:**

- Do NOT change the logo `<picture>` block or banner image (those come from Phase 20/21)
- Do NOT add example videos (Tuireel doesn't have them yet)
- Do NOT link to `tuireel.dev` anywhere — all docs links must go to `https://tuireel.micr.dev`
  </action>
  <verify>

1. `grep -c 'tuireel\.dev[^.]' README.md` returns 0 (no stale domain — note the [^.] to exclude tuireel.dev.micr patterns)
2. `grep -c 'tuireel\.micr\.dev' README.md` returns at least 1
3. `grep -c '"value"' README.md` returns 0 (bug fixed)
4. `grep -c 'Brand system' README.md` returns 0 (internal link removed)
5. README contains sections in order: Quick Start, Usage, Step Types, Config, Development, Packages, License
6. All step types from research (launch, type, press, wait, pause, scroll, click, screenshot, resize, set-env) appear in the Step Types table
7. All CLI commands (init, validate, record, preview, composite) appear in Usage section
   </verify>
   <done>README.md is restructured to match Webreel's pattern (logo → tagline → badges → description + docs link → Quick Start → Usage with CLI reference + step types + config → Development → Packages → License). All CLI commands, flags, and step types match docs/cli-reference.mdx and docs/steps-reference.mdx. No stale `tuireel.dev` references. No `"value"` field bug. No brand-system internal link.</done>
   </task>

<task type="auto">
  <name>Task 2: Repo-wide link sweep and verification</name>
  <files>README.md</files>
  <action>
Run a repo-wide link sweep to confirm no stale docs-domain references remain in user-facing files. This is a verification task — research found only one stale link (README line 76) which Task 1 already fixes.

**Sweep these file patterns:**

```bash
grep -rn 'tuireel\.dev' --include='*.md' --include='*.mdx' --include='*.json' --include='*.yaml' --include='*.yml' --include='*.ts' --include='*.js' . | grep -v node_modules | grep -v '.planning/'
```

**Expected result:** ZERO matches. The only stale reference was in README.md (fixed by Task 1). All `.planning/` references are historical and must NOT be modified.

**If unexpected stale references found:**

- Fix them by replacing `tuireel.dev` with `tuireel.micr.dev` in user-facing files only
- Do NOT modify any file under `.planning/` (historical records)

**Also verify clean files:**

- `CONTRIBUTING.md` — should have no docs-domain references (confirmed clean in research)
- `docs/docs.json` — should have no `tuireel.dev` references (confirmed clean in research)
- `docs/*.mdx` — should have no `tuireel.dev` references (confirmed clean in research)
- Root `package.json` — homepage points to GitHub, not docs domain (confirmed clean)
- `packages/core/package.json` and `packages/cli/package.json` — no homepage field (confirmed clean)
  </action>
  <verify>

1. `grep -rn 'tuireel\.dev' --include='*.md' --include='*.mdx' --include='*.json' --include='*.yaml' --include='*.yml' --include='*.ts' --include='*.js' . | grep -v node_modules | grep -v '.planning/' | wc -l` returns 0
2. `grep -rn 'tuireel\.micr\.dev' README.md | wc -l` returns at least 1
   </verify>
   <done>Repo-wide link sweep confirms zero stale `tuireel.dev` references in any user-facing file. All docs links point to `https://tuireel.micr.dev`. Historical `.planning/` references are untouched.</done>
   </task>

</tasks>

<verification>
1. README structure matches Webreel pattern: logo → badges → description + docs link → Quick Start → Usage → Development → Packages → License
2. Every CLI command and flag in the README is accurate per docs/cli-reference.mdx
3. Every step type in the README matches docs/steps-reference.mdx
4. `grep -rn 'tuireel\.dev' . | grep -v node_modules | grep -v '.planning/'` returns zero matches
5. `grep 'tuireel\.micr\.dev' README.md` returns at least one match
6. No `"value"` field in any JSONC example (bug fixed)
7. No `Brand system` link in README
</verification>

<success_criteria>

- README.md restructured to Webreel pattern with accurate Tuireel CLI content
- All in-repo user-facing links point to `https://tuireel.micr.dev` (zero stale domains)
- JSONC example bug (`"value"` → `"text"`, `"enter"` step → `"press"`) is fixed
- Brand-system internal link removed from user-facing README
  </success_criteria>

<output>
After completion, create `.planning/phases/24-readme-refresh-link-hygiene/24-01-SUMMARY.md`
</output>
