---
phase: 25-timing-contract-and-profiles
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/config/schema.ts
  - packages/core/src/config/loader.ts
  - packages/core/src/config/index.ts
  - packages/core/src/config/generate-schema.ts
  - packages/core/src/delivery-profiles/built-in.ts
  - packages/core/src/delivery-profiles/resolve.ts
  - packages/core/src/delivery-profiles/index.ts
  - packages/core/test/delivery-profiles.test.ts
  - packages/core/test/config.test.ts
autonomous: true
requirements:
  - CAP-01
  - READ-03
must_haves:
  truths:
    - Author can choose a named delivery profile without losing separate raw capture and final output cadence controls.
    - Explicit fps and captureFps values override delivery-profile defaults instead of being silently rewritten.
    - Visual presets and delivery profiles stack cleanly, so preset keeps look/sound semantics while deliveryProfile owns timing/readability defaults.
  artifacts:
    - path: packages/core/src/delivery-profiles/built-in.ts
      provides: named delivery-profile definitions with timing and readability defaults
    - path: packages/core/src/delivery-profiles/resolve.ts
      provides: shared delivery-profile merge logic with explicit-config precedence
    - path: packages/core/src/config/schema.ts
      provides: public config contract for deliveryProfile, fps, and captureFps
    - path: packages/core/test/delivery-profiles.test.ts
      provides: regression coverage for profile resolution and override rules
  key_links:
    - from: packages/core/src/config/loader.ts
      to: packages/core/src/delivery-profiles/resolve.ts
      via: config loading applies delivery-profile defaults before schema parsing
      pattern: resolveDeliveryProfile
    - from: packages/core/src/config/schema.ts
      to: packages/core/src/delivery-profiles/built-in.ts
      via: deliveryProfile enum/validation source
      pattern: deliveryProfile
---

<objective>
Define the core delivery-profile contract so Phase 25 has one authoritative place for output cadence, capture cadence, and readability defaults.

Purpose: Lock CAP-01 and the core half of READ-03 before recorder/composite work builds on top of it.
Output: Delivery-profile modules, schema/loader wiring, generated schema updates, and regression tests.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-timing-contract-and-profiles/25-CONTEXT.md
@.planning/phases/25-timing-contract-and-profiles/25-RESEARCH.md
@.planning/phases/08-presets-reliability/08-01-SUMMARY.md
@.planning/phases/06-workflow-polish/06-03-SUMMARY.md
@packages/core/src/config/schema.ts
@packages/core/src/config/loader.ts
@packages/core/src/presets/built-in.ts
@packages/core/src/presets/resolve.ts

<interfaces>
From `packages/core/src/presets/built-in.ts`:

```ts
export type PresetConfig = Partial<Omit<TuireelConfig, "steps" | "output" | "$schema" | "format">>;
```

From `packages/core/src/presets/resolve.ts`:

```ts
export function resolvePreset(rawConfig: Record<string, unknown>): Record<string, unknown>;
```

From `packages/core/src/config/schema.ts`:

```ts
const baseConfigFields = {
  preset: z.enum(PRESET_NAMES).optional(),
  format: z.enum(outputFormats).default("mp4"),
  output: z.string().default("output.mp4"),
  fps: z.number().int().positive().default(30),
  captureFps: z.number().int().positive().optional(),
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delivery-profile resolution tests first</name>
  <files>packages/core/test/delivery-profiles.test.ts, packages/core/test/config.test.ts</files>
  <behavior>
    - Test 1: `deliveryProfile` applies named timing/readability defaults while leaving `preset` as a separate visual layer.
    - Test 2: explicit `fps`, `captureFps`, and readability fields override delivery-profile defaults.
    - Test 3: unknown `deliveryProfile` values or invalid delivery-profile shapes fail with actionable validation guidance.
  </behavior>
  <action>Create failing unit coverage for delivery-profile resolution before implementation. Cover single-video and multi-video/defaults inputs, generated schema visibility for `deliveryProfile`, and the user-locked rule that explicit config values win. Do not use mocks; keep tests against the real loader/schema path.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- delivery-profiles.test.ts config.test.ts</automated>
  </verify>
  <done>Tests describe the delivery-profile contract clearly enough that implementation can be completed without rereading the phase discussion.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stackable delivery-profile modules and loader precedence</name>
  <files>packages/core/src/delivery-profiles/built-in.ts, packages/core/src/delivery-profiles/resolve.ts, packages/core/src/delivery-profiles/index.ts, packages/core/src/config/schema.ts, packages/core/src/config/loader.ts, packages/core/src/config/index.ts, packages/core/src/config/generate-schema.ts</files>
  <action>Create a new delivery-profile layer separate from visual presets. Use outcome-based names and include timing plus readability defaults in each built-in profile; keep `preset` behavior unchanged per user decision. Add `deliveryProfile` to single-video and multi-video authoring surfaces, resolve profile defaults in the shared config-loading path, and preserve explicit author fields as the final authority. Keep `fps` as final delivery cadence, keep `captureFps` as raw capture cadence, and do not add a `captureFps &gt;= fps` restriction because existing two-pass configs rely on lower raw cadence. Export the new resolver/types from core so downstream plans can consume them without re-implementing merge logic.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- delivery-profiles.test.ts config.test.ts</automated>
  </verify>
  <done>`deliveryProfile` is a first-class public config field, delivery-profile defaults resolve through one shared helper, and the schema/loader path reflects profile-first authoring without overloading `preset`.</done>
</task>

</tasks>

<verification>
Run the focused core tests first, then confirm generated schema coverage still passes through the existing config suite.
</verification>

<success_criteria>

- `deliveryProfile` exists in the public config contract and is validated from the same schema path used by `record`, `watch`, and `composite`.
- Named delivery profiles set timing/readability defaults, but explicit `fps` and `captureFps` overrides still win.
- Visual presets remain visual-only and can be combined with delivery profiles without semantic drift.
  </success_criteria>

<output>
After completion, create `.planning/phases/25-timing-contract-and-profiles/25-01-SUMMARY.md`
</output>
