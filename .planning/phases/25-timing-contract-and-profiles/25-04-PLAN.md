---
phase: 25-timing-contract-and-profiles
plan: 04
type: execute
wave: 3
depends_on:
  - 25-03
files_modified:
  - packages/core/src/__tests__/compositor.test.ts
  - packages/cli/src/commands/composite.ts
  - packages/cli/test/commands.test.ts
autonomous: true
requirements:
  - CAP-04
must_haves:
  truths:
    - Re-running `composite` preserves the recorded timing feel by default instead of silently adopting new timing settings.
    - Incompatible timing-affecting recomposite requests fail with clear guidance to re-record, while packaging-only changes still work.
    - Legacy timelines without explicit timing metadata are handled conservatively instead of silently changing pacing.
  artifacts:
    - path: packages/cli/src/commands/composite.ts
      provides: recomposite compatibility gate against saved timing metadata
    - path: packages/cli/test/commands.test.ts
      provides: CLI regression coverage for rerun guidance and packaging-only recomposite behavior
    - path: packages/core/src/__tests__/compositor.test.ts
      provides: core compose-path coverage for packaging-only versus timing-affecting recomposite inputs
  key_links:
    - from: packages/cli/src/commands/composite.ts
      to: packages/core/src/timeline/timing-contract.ts
      via: composite validates resolved config against saved timing contract before compose()
      pattern: isCompatibleTiming
---

<objective>
Enforce the saved timing contract in `composite` so recomposition preserves the original pacing intent by default.

Purpose: Finish CAP-04 after Plan 03 lands the persisted contract, keeping CLI/composite behavior small enough to execute without context bloat.
Output: Composite command enforcement, command-path regression tests, and packaging-only compatibility coverage.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-timing-contract-and-profiles/25-CONTEXT.md
@.planning/phases/25-timing-contract-and-profiles/25-RESEARCH.md
@.planning/phases/25-timing-contract-and-profiles/25-03-PLAN.md
@packages/cli/src/commands/composite.ts
@packages/cli/test/commands.test.ts
@packages/core/src/timeline/timing-contract.ts

<interfaces>
From `packages/core/src/timeline/timing-contract.ts` after Plan 03:

```ts
export interface TimingContract { /* versioned persisted timing metadata */ }
export function isCompatibleTiming(...): ...;
```

From `packages/cli/src/commands/composite.ts`:

```ts
const timelineData = JSON.parse(await readFile(artifacts.timelinePath, "utf8")) as TimelineData;
await compose(artifacts.rawVideoPath, timelineData, outputPath, { ... });
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock recomposite timing enforcement behavior in tests</name>
  <files>packages/core/src/__tests__/compositor.test.ts, packages/cli/test/commands.test.ts</files>
  <behavior>
    - Test 1: compatible packaging-only recomposite inputs pass without forcing a re-record.
    - Test 2: timing-affecting recomposite changes fail with guidance to rerun `tuireel record`.
    - Test 3: legacy timelines without timingContract allow overlay-only recomposite but reject timing changes.
  </behavior>
  <action>Create failing tests first for the command-path guidance and allowed packaging-only recomposite behavior. Reuse the persisted timing contract from Plan 03 instead of redefining artifact shapes here. Keep coverage on real compose/composite boundaries; do not use mocks.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- compositor.test.ts &amp;&amp; pnpm --filter tuireel test -- commands.test.ts</automated>
  </verify>
  <done>The user-facing recomposite enforcement rules are pinned in core and CLI tests before command changes land.</done>
</task>

<task type="auto">
  <name>Task 2: Reject timing drift while allowing packaging-only recomposite changes</name>
  <files>packages/cli/src/commands/composite.ts</files>
  <action>Before calling `compose()`, load the saved timeline contract and compare it with the currently resolved config using the shared helper from Plan 03. Reject timing-affecting mismatches such as changed `fps`, changed `captureFps`, or any future timing fields surfaced by the helper, and tell the author to re-run `tuireel record` when they actually want different pacing. Allow packaging/readability changes that do not alter timing intent, including format, sound, cursor/HUD visibility, cursor size, trim-leading-static, output sizing, and theme/background-driven packaging. For legacy timelines with no explicit timingContract, allow overlay-only recomposite but fail timing changes with the same rerun guidance. Also bring `composite` parity up to date by passing through any compatible packaging fields already honored by `record`.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- compositor.test.ts &amp;&amp; pnpm --filter tuireel test -- commands.test.ts</automated>
  </verify>
  <done>`composite` preserves recorded pacing by default, surfaces timing mismatches clearly, and still supports packaging-only recomposition from saved artifacts.</done>
</task>

</tasks>

<verification>
Run the focused compositor/core test first, then the CLI suite to prove the command path and saved timing contract stay aligned.
</verification>

<success_criteria>

- `composite` refuses timing-affecting drift and explains when a fresh recording is required.
- Packaging-only recomposition still works against saved artifacts.
- Legacy artifact handling is explicit instead of silently guessing timing compatibility.
  </success_criteria>

<output>
After completion, create `.planning/phases/25-timing-contract-and-profiles/25-04-SUMMARY.md`
</output>
