---
phase: 25-timing-contract-and-profiles
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - packages/cli/src/commands/init.ts
  - packages/cli/test/commands.test.ts
  - README.md
  - docs/config-reference.mdx
  - docs/presets.mdx
  - docs/quickstart.mdx
  - docs/multi-video.mdx
  - skills/tuireel/examples.md
autonomous: true
requirements:
  - READ-03
  - CAP-01
must_haves:
  truths:
    - Authors see delivery profiles as the default workflow in starter config and docs.
    - Authors can understand that `fps` is final output cadence and `captureFps` is raw capture cadence without reading source code.
    - Authors can combine a visual preset with a delivery profile in examples and reference docs.
  artifacts:
    - path: packages/cli/src/commands/init.ts
      provides: profile-first starter config and init prompts
    - path: packages/cli/test/commands.test.ts
      provides: CLI regression coverage for starter config/schema output
    - path: docs/config-reference.mdx
      provides: authoritative public docs for timing fields and deliveryProfile
    - path: README.md
      provides: top-level config semantics aligned with the new contract
  key_links:
    - from: packages/cli/src/commands/init.ts
      to: packages/core/src/config/schema.ts
      via: generated starter config uses the public deliveryProfile contract
      pattern: deliveryProfile
    - from: README.md
      to: docs/config-reference.mdx
      via: shared wording for fps vs captureFps semantics
      pattern: captureFps
---

<objective>
Expose delivery profiles through the author-facing CLI and docs so the new timing contract is teachable, discoverable, and consistent.

Purpose: Turn the core contract from Plan 01 into a usable profile-first workflow that satisfies READ-03 for real authors.
Output: Updated `init` scaffolding, CLI regression tests, and contract-aligned docs/examples.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-timing-contract-and-profiles/25-CONTEXT.md
@.planning/phases/25-timing-contract-and-profiles/25-RESEARCH.md
@.planning/phases/10-documentation/10-01-SUMMARY.md
@packages/cli/src/commands/init.ts
@packages/cli/test/commands.test.ts
@README.md
@docs/config-reference.mdx

<interfaces>
From `packages/cli/src/commands/init.ts`:

```ts
function createStarterConfig(schemaPath: string, preset?: string): string;
```

Current starter config excerpt:

```jsonc
{
  "$schema": "...",
  "output": "demo.mp4",
  "fps": 30,
  "cols": 80,
  "rows": 24,
  "steps": [...]
}
```

From `README.md` / `docs/config-reference.mdx`:

```md
| `fps` | `30` | capture frame rate |
```

This wording is drifted and must be corrected in every touched author-facing surface.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CLI coverage for profile-first starter config</name>
  <files>packages/cli/test/commands.test.ts</files>
  <behavior>
    - Test 1: `tuireel init` writes a starter config that includes `deliveryProfile` and keeps `preset` optional/stackable.
    - Test 2: the generated schema persisted by `init` includes `deliveryProfile` and `captureFps` in the authoring contract.
    - Test 3: starter config still validates through the real config parser.
  </behavior>
  <action>Extend the existing CLI integration test file first. Keep coverage focused on starter config shape and persisted schema output; do not mock prompts or filesystem behavior.</action>
  <verify>
    <automated>pnpm --filter tuireel test -- commands.test.ts</automated>
  </verify>
  <done>The expected profile-first init behavior is locked in tests before the command or docs change.</done>
</task>

<task type="auto">
  <name>Task 2: Make `tuireel init` teach deliveryProfile first</name>
  <files>packages/cli/src/commands/init.ts</files>
  <action>Update the starter-config flow so delivery profile is the default authoring path. Scaffold `deliveryProfile` into the generated config, keep `preset` as a separate optional visual choice, and ensure the starter example still validates in non-TTY runs. If the command prompts interactively, ask for delivery profile before preset so the workflow matches the locked user decision. Keep the generated config simple: do not dump low-level timing knobs unless needed to demonstrate explicit overrides.</action>
  <verify>
    <automated>pnpm --filter tuireel test -- commands.test.ts</automated>
  </verify>
  <done>`tuireel init` produces a starter config that nudges authors toward named delivery profiles instead of hand-tuning fps fields first.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite docs and examples around the new timing contract</name>
  <files>README.md, docs/config-reference.mdx, docs/presets.mdx, docs/quickstart.mdx, docs/multi-video.mdx, skills/tuireel/examples.md</files>
  <action>Update every touched author-facing surface to align on the same contract: `fps` means final output cadence, `captureFps` means raw capture cadence, `deliveryProfile` is the named outcome-based bundle for timing plus readability defaults, and `preset` remains a separate visual layer that can be stacked. Replace any wording that still teaches `fps` as capture rate. Add at least one stacked example using both `preset` and `deliveryProfile`, and keep examples consistent across README, docs, and skill examples.</action>
  <verify>
    <automated>pnpm --filter tuireel test -- commands.test.ts &amp;&amp; rg "deliveryProfile|captureFps|final output cadence|raw capture cadence" README.md docs/config-reference.mdx docs/presets.mdx docs/quickstart.mdx docs/multi-video.mdx skills/tuireel/examples.md</automated>
  </verify>
  <done>README, docs, and examples all teach the same profile-first contract with no lingering claim that `fps` is capture rate.</done>
</task>

</tasks>

<verification>
Use the CLI test suite as the fast loop, then run the grep check to prove the doc surfaces actually picked up the new terminology.
</verification>

<success_criteria>

- `tuireel init` scaffolds a delivery-profile-first config that still validates out of the box.
- README and docs describe `fps` and `captureFps` correctly and consistently.
- At least one author-facing example shows `preset` and `deliveryProfile` used together.
  </success_criteria>

<output>
After completion, create `.planning/phases/25-timing-contract-and-profiles/25-02-SUMMARY.md`
</output>
