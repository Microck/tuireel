---
phase: 25-timing-contract-and-profiles
plan: 03
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - packages/core/src/timeline/types.ts
  - packages/core/src/timeline/interaction-timeline.ts
  - packages/core/src/timeline/timing-contract.ts
  - packages/core/src/recorder.ts
  - packages/core/src/index.ts
  - packages/core/test/timing-contract.test.ts
  - packages/core/src/timeline/__tests__/interaction-timeline.test.ts
autonomous: true
requirements:
  - CAP-04
must_haves:
  truths:
    - Saved recordings expose an inspectable timing contract with output cadence, raw capture cadence, and frame-count facts.
    - Saved timelines carry enough explicit timing metadata that recomposite enforcement can compare intent without inferring from ffprobe output or source code.
    - Legacy timelines remain loadable with explicit fallback rules instead of silent timing guesses.
  artifacts:
    - path: packages/core/src/timeline/timing-contract.ts
      provides: timing-contract type and compatibility helper
    - path: packages/core/src/timeline/types.ts
      provides: serialized timeline artifact contract including timingContract
    - path: packages/core/src/recorder.ts
      provides: record-time timing-contract persistence
    - path: packages/core/test/timing-contract.test.ts
      provides: regression coverage for serialized timing facts and compatibility helper rules
  key_links:
    - from: packages/core/src/recorder.ts
      to: packages/core/src/timeline/types.ts
      via: record writes timingContract into saved timeline JSON
      pattern: timingContract
---

<objective>
Persist an explicit timing contract in saved artifacts so recomposite enforcement has one inspectable source of truth.

Purpose: Land the persistence half of CAP-04 first so the later CLI/composite plan can enforce timing drift without re-defining artifact semantics.
Output: Timeline contract extension, compatibility helper, recorder wiring, and core regression tests.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-timing-contract-and-profiles/25-CONTEXT.md
@.planning/phases/25-timing-contract-and-profiles/25-RESEARCH.md
@.planning/phases/04-overlay-system/04-05-SUMMARY.md
@packages/core/src/timeline/types.ts
@packages/core/src/timeline/interaction-timeline.ts
@packages/core/src/recorder.ts

<interfaces>
From `packages/core/src/timeline/types.ts`:

```ts
export interface TimelineData {
  fps: number;
  width: number;
  height: number;
  frameCount: number;
  theme: TimelineTheme;
  frames: FrameData[];
  events: SoundEvent[];
  terminalFrames?: number[];
}
```

From `packages/core/src/timeline/interaction-timeline.ts`:

```ts
toJSON(): TimelineData;
save(path: string): void;
static load(json: TimelineData): InteractionTimeline;
```

From `packages/cli/src/commands/composite.ts`:

```ts
const timelineData = JSON.parse(await readFile(artifacts.timelinePath, "utf8")) as TimelineData;
await compose(artifacts.rawVideoPath, timelineData, outputPath, { ... });
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock serialized timing-contract behavior in core tests</name>
  <files>packages/core/test/timing-contract.test.ts, packages/core/src/timeline/__tests__/interaction-timeline.test.ts</files>
  <behavior>
    - Test 1: saved timeline JSON contains explicit timingContract fields for output fps, capture fps, wall-clock duration, and frame counts.
    - Test 2: the shared compatibility helper distinguishes timing-affecting changes from packaging-only changes using the persisted contract.
    - Test 3: legacy timelines without timingContract return an explicit fallback result that later CLI enforcement can honor.
  </behavior>
  <action>Create failing tests first for the artifact contract and shared compatibility helper. Keep this plan scoped to core serialization and comparison semantics only: do not edit CLI expectations here. Use real timeline serialization paths instead of mocks so the persisted contract is enforced at the same boundary `record` uses.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- timing-contract.test.ts interaction-timeline.test.ts</automated>
  </verify>
  <done>The serialized timing-contract shape and helper semantics are expressed in tests before recorder wiring changes begin.</done>
</task>

<task type="auto">
  <name>Task 2: Persist explicit timingContract metadata in timeline artifacts</name>
  <files>packages/core/src/timeline/types.ts, packages/core/src/timeline/interaction-timeline.ts, packages/core/src/timeline/timing-contract.ts, packages/core/src/recorder.ts, packages/core/src/index.ts</files>
  <action>Add a versioned `timingContract` object to the serialized timeline boundary and keep `TimelineData.fps` as the backward-compatible output-fps alias. Create a shared helper module that builds and compares timing contracts so future pacing fields can extend the same comparison logic. Record-time metadata must include resolved output fps, resolved capture fps, wall-clock duration in milliseconds, raw frame count, output frame count, terminal frame count, and the selected `deliveryProfile` name when present. Export the helper/types from core so `composite` and later diagnostics plans use one source of truth.</action>
  <verify>
    <automated>pnpm --filter @tuireel/core test -- timing-contract.test.ts interaction-timeline.test.ts</automated>
  </verify>
  <done>Saved `.timeline.json` artifacts expose explicit timing metadata that can be inspected without ffprobe or source-code knowledge.</done>
</task>

</tasks>

<verification>
Run the focused core tests after each task to prove the persisted contract and helper stay aligned before CLI enforcement builds on them.
</verification>

<success_criteria>

- Timeline artifacts serialize an explicit, versioned timingContract object.
- Saved timing metadata includes enough facts for later recomposite checks to compare timing intent explicitly.
- Legacy artifact handling is explicit instead of silently guessing timing compatibility.
  </success_criteria>

<output>
After completion, create `.planning/phases/25-timing-contract-and-profiles/25-03-SUMMARY.md`
</output>
