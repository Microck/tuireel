---
phase: 14-verify-phases-10-12
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-release-performance/12-VERIFICATION.md
  - .planning/phases/14-verify-phases-10-12/14-03-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`.planning/phases/12-release-performance/12-VERIFICATION.md` exists and records evidence for Phase 12 ROADMAP success criteria"
    - "Known publish/installability gaps are recorded honestly (owned by Phase 16)"
  artifacts:
    - path: ".planning/phases/12-release-performance/12-VERIFICATION.md"
      provides: "Phase 12 verification report with status + evidence"
      contains: "phase: 12-release-performance"
    - path: ".github/workflows/release.yml"
      provides: "Release workflow definition"
      contains: "name: Release"
    - path: "scripts/benchmark.ts"
      provides: "Benchmark script used for performance baseline"
      contains: "pnpm benchmark"
  key_links:
    - from: ".planning/phases/12-release-performance/12-VERIFICATION.md"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "Evidence references published installability failure"
      pattern: "workspace:\\*"
---

<objective>
Create the missing Phase 12 verification report so milestone audit verification coverage passes.

Purpose: Close Phase 14 gap for Phase 12 (work done; verification artifact missing).
Output: `.planning/phases/12-release-performance/12-VERIFICATION.md` with concrete evidence and an accurate status.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/workflows/verify-phase.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/12-release-performance/12-01-SUMMARY.md
@.planning/phases/12-release-performance/12-02-SUMMARY.md
@.planning/phases/12-release-performance/12-03-SUMMARY.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md
@.github/workflows/release.yml
@.changeset/config.json
@package.json
@scripts/benchmark.ts
@packages/core/src/compositor.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect Phase 12 evidence (changesets + release workflow + benchmark)</name>
  <files>.planning/phases/12-release-performance/12-VERIFICATION.md</files>
  <action>
Collect evidence for each Phase 12 ROADMAP success criterion, but do not fix known publish/installability gaps (Phase 16 owns them).

1. Changesets setup evidence:
   - Verify `.changeset/config.json` exists and references both `@tuireel/core` and `tuireel` in a fixed versioning group.
   - Run: `pnpm changeset status` (capture representative output lines).

2. Release workflow evidence:
   - Verify `.github/workflows/release.yml` exists and uses `changesets/action@v1`.
   - Record the publish command (`pnpm release`) and note any drift between summary claims and workflow comments (OIDC trusted publishing vs token).

3. Post-publish installability evidence:
   - If network access is available, run: `npm view tuireel dependencies` and capture evidence of `@tuireel/core: workspace:*` (expected current gap).
   - If you cannot access npm from this environment, cite the existing evidence snapshot in `.planning/v1.1-MILESTONE-AUDIT.md` and record this as a known gap with reproduction steps.

4. Performance benchmark evidence:
   - Run: `pnpm benchmark` and capture the results lines (recording time, compositing time, ms/frame).
   - Cite the compositor optimization in `packages/core/src/compositor.ts` (overlay skip via `copyFile` when no overlays).
     </action>
     <verify>
     `test -f .changeset/config.json && test -f .github/workflows/release.yml`

`pnpm changeset status`

`pnpm benchmark`
</verify>
<done>Evidence is collected for changesets config, release workflow existence, benchmark measurability, and the known installability gap is documented with concrete reproduction steps.</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 12 verification report (numeric filename)</name>
  <files>.planning/phases/12-release-performance/12-VERIFICATION.md</files>
  <action>
Create `.planning/phases/12-release-performance/12-VERIFICATION.md` using the canonical template at `@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md`.

Requirements for the report:

- YAML frontmatter:
  - `phase: 12-release-performance`
  - `verified: <ISO timestamp>`
  - `status: passed | gaps_found | human_needed` (expected: `gaps_found` due to `npx tuireel` installability failure)
  - `score: N/M must-haves verified`

- Observable Truths table MUST mirror Phase 12 success criteria from `.planning/ROADMAP.md`:
  1. Changeset merge creates "Version Packages" PR
  2. Version PR merge publishes packages + creates GitHub release
  3. `npx tuireel` and `bunx tuireel` install+run after publish
  4. Benchmark exists and reports timing breakdown
  5. At least one compositing optimization is applied and measurable

- Report MUST explicitly state which truths are verifiable locally (benchmark + code inspection) vs which require external system verification (GitHub Actions runs, npm publish/install).

- Include the known `workspace:*` dependency publish bug as a critical gap and attribute remediation to Phase 16 (do not attempt fixes here).

Important: Use numeric report filename `12-VERIFICATION.md`.
</action>
<verify>
`test -f .planning/phases/12-release-performance/12-VERIFICATION.md`

`node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/12-release-performance/12-VERIFICATION.md','utf8'); process.exit(s.includes('phase: 12-release-performance')?0:1)"`
</verify>
<done>`12-VERIFICATION.md` exists, uses the canonical template structure, and records Phase 12 truths with an honest gap assessment.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/12-release-performance/12-VERIFICATION.md`
- Report contains benchmark evidence and records publish/installability gaps without papering them over.
</verification>

<success_criteria>

- Phase 12 verification report exists at `.planning/phases/12-release-performance/12-VERIFICATION.md`.
- Report status is accurate and lists concrete steps to reproduce the installability failure.
  </success_criteria>

<output>
After completion, create `.planning/phases/14-verify-phases-10-12/14-03-SUMMARY.md`
</output>
