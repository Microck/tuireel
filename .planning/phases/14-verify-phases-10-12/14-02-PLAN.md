---
phase: 14-verify-phases-10-12
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/11-ci-linting/11-VERIFICATION.md
  - .planning/phases/14-verify-phases-10-12/14-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "`.planning/phases/11-ci-linting/11-VERIFICATION.md` exists and records evidence for Phase 11 ROADMAP success criteria"
    - "Known CI wiring gaps are recorded honestly (owned by Phase 15)"
  artifacts:
    - path: ".planning/phases/11-ci-linting/11-VERIFICATION.md"
      provides: "Phase 11 verification report with status + evidence"
      contains: "phase: 11-ci-linting"
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow definition"
      contains: "name: CI"
    - path: ".github/workflows/video-smoke.yml"
      provides: "Video smoke workflow definition"
      contains: "name: Video Smoke Tests"
  key_links:
    - from: ".planning/phases/11-ci-linting/11-VERIFICATION.md"
      to: ".planning/v1.1-MILESTONE-AUDIT.md"
      via: "Evidence references known wiring gaps"
      pattern: "CI type-check step does not type-check"
---

<objective>
Create the missing Phase 11 verification report so milestone audit verification coverage passes.

Purpose: Close Phase 14 gap for Phase 11 (work done; verification artifact missing).
Output: `.planning/phases/11-ci-linting/11-VERIFICATION.md` with concrete evidence and an accurate status.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/workflows/verify-phase.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/11-ci-linting/11-01-SUMMARY.md
@.planning/phases/11-ci-linting/11-02-SUMMARY.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md
@.github/workflows/ci.yml
@.github/workflows/video-smoke.yml
@eslint.config.mjs
@.prettierrc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Collect Phase 11 evidence (local scripts + workflow inspection)</name>
  <files>.planning/phases/11-ci-linting/11-VERIFICATION.md</files>
  <action>
Collect evidence for each Phase 11 ROADMAP success criterion, but do not fix known wiring gaps (Phase 15 owns them).

0. Verify workflow triggers exist (push + pull_request) and capture evidence into `11-VERIFICATION.md`:
   - Inspect `.github/workflows/ci.yml` `on:` section includes `push` and `pull_request`
   - Inspect `.github/workflows/video-smoke.yml` `on:` section includes `push` and `pull_request`

1. Run baseline checks and capture key output lines:
   - `pnpm lint`
   - `pnpm build`
   - `pnpm test`

2. Reproduce the known type-check wiring issue to support an honest `gaps_found` status (expected failure mode):
   - Run: `pnpm exec tsc --noEmit`
   - Capture evidence if it prints the TypeScript help text / does not reference a project config.
   - Cross-reference `.planning/v1.1-MILESTONE-AUDIT.md` integration gap text.

3. Inspect the video smoke workflow wiring and capture evidence of the current invocation:
   - `.github/workflows/video-smoke.yml` uses `pnpm exec tuireel record ...`
   - If possible, run `pnpm exec tuireel --help` from repo root and capture the failure output (expected: command not found).
     </action>
     <verify>
     `pnpm lint && pnpm build && pnpm test`

`rg -n "^\\s*on:\\s*$" .github/workflows/ci.yml && rg -n "^\\s*(push|pull_request):\\s*$" .github/workflows/ci.yml`

`rg -n "^\\s*on:\\s*$" .github/workflows/video-smoke.yml && rg -n "^\\s*(push|pull_request):\\s*$" .github/workflows/video-smoke.yml`

`rg -n "pnpm exec tsc --noEmit" .github/workflows/ci.yml`

`rg -n "pnpm exec tuireel record" .github/workflows/video-smoke.yml`
</verify>
<done>Evidence is collected for lint/build/test, plus concrete reproduction evidence exists for the two known CI wiring gaps (type-check misconfigured and video smoke invocation broken).</done>
</task>

<task type="auto">
  <name>Task 2: Write Phase 11 verification report (numeric filename)</name>
  <files>.planning/phases/11-ci-linting/11-VERIFICATION.md</files>
  <action>
Create `.planning/phases/11-ci-linting/11-VERIFICATION.md` using the canonical template at `@/home/ubuntu/.config/opencode/get-shit-done/templates/verification-report.md`.

Requirements for the report:

- YAML frontmatter:
  - `phase: 11-ci-linting`
  - `verified: <ISO timestamp>`
  - `status: passed | gaps_found | human_needed` (expected: `gaps_found` due to known integration gaps)
  - `score: N/M must-haves verified`

- Observable Truths table MUST mirror Phase 11 success criteria from `.planning/ROADMAP.md`:
  1.  CI triggers on push/PR and runs lint/type-check/build/test
  2.  ESLint + Prettier configs exist and `pnpm lint` passes
  3.  Video smoke workflow records MP4/WebM/GIF and validates with ffprobe
  4.  Artifacts upload on failure

- For truth #1, report MUST include evidence that both workflows are configured to run on `push` and `pull_request` (cite the relevant `on:` snippets from `.github/workflows/ci.yml` and `.github/workflows/video-smoke.yml`).

- Report MUST explicitly separate:
  - What is verified locally (lint/build/test)
  - What is blocked by known wiring gaps (type-check without tsconfig; `pnpm exec tuireel` invocation)

- Reference `.planning/v1.1-MILESTONE-AUDIT.md` for the integration gaps, and mark them as critical gaps with remediation owned by Phase 15 (do not propose fixes beyond linking to Phase 15).

Important: Use numeric report filename `11-VERIFICATION.md`.
</action>
<verify>
`test -f .planning/phases/11-ci-linting/11-VERIFICATION.md`

`node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/phases/11-ci-linting/11-VERIFICATION.md','utf8'); process.exit(s.includes('phase: 11-ci-linting')?0:1)"`
</verify>
<done>`11-VERIFICATION.md` exists, uses the canonical template structure, and records Phase 11 truths with an honest gap assessment.</done>
</task>

</tasks>

<verification>
- `test -f .planning/phases/11-ci-linting/11-VERIFICATION.md`
- Report includes explicit evidence for `pnpm lint` passing and records the known wiring gaps from the milestone audit.
</verification>

<success_criteria>

- Phase 11 verification report exists at `.planning/phases/11-ci-linting/11-VERIFICATION.md`.
- Report accurately reflects current state (likely `gaps_found`) and does not silently claim broken type-check/smoke wiring is passing.
  </success_criteria>

<output>
After completion, create `.planning/phases/14-verify-phases-10-12/14-02-SUMMARY.md`
</output>
