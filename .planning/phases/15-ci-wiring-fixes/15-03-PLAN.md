---
phase: 15-ci-wiring-fixes
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified:
  - .planning/v1.1-MILESTONE-AUDIT.md
  - .planning/phases/15-ci-wiring-fixes/15-audit-milestone.md
  - .planning/phases/15-ci-wiring-fixes/15-03-SUMMARY.md
autonomous: false

must_haves:
  truths:
    - "Re-running /gsd-audit-milestone no longer flags CI-01 or CI-04 as unsatisfied"
    - "Audit rerun evidence is recorded in a Phase 15 artifact"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow uses real type-check"
      contains: "tsc --noEmit -p"
    - path: ".github/workflows/video-smoke.yml"
      provides: "Video smoke workflow invokes CLI reliably"
      contains: "node packages/cli/dist/index.js"
    - path: ".planning/v1.1-MILESTONE-AUDIT.md"
      provides: "Milestone v1.1 audit output"
      contains: "milestone: v1.1"
    - path: ".planning/phases/15-ci-wiring-fixes/15-audit-milestone.md"
      provides: "Evidence that CI-01 and CI-04 gaps are no longer present"
      contains: "gsd-audit-milestone"
  key_links:
    - from: ".planning/v1.1-MILESTONE-AUDIT.md"
      to: "CI-01 unsatisfied gap"
      via: "gaps.integration list"
      pattern: "CI type-check step does not type-check"
    - from: ".planning/v1.1-MILESTONE-AUDIT.md"
      to: "CI-04 unsatisfied gap"
      via: "gaps.integration list"
      pattern: "Video smoke workflow invokes a non-existent CLI binary"
---

<objective>
Re-run the milestone audit and record evidence that the CI wiring gaps (CI-01 and CI-04) are no longer flagged.

Purpose: Close Phase 15 success criterion #3 (audit no longer flags CI-01/CI-04).
Output: Updated `.planning/v1.1-MILESTONE-AUDIT.md` plus `.planning/phases/15-ci-wiring-fixes/15-audit-milestone.md` evidence note.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/workflows/audit-milestone.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.github/workflows/ci.yml
@.github/workflows/video-smoke.yml
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Re-run milestone audit for v1.1</action>
  <instructions>
This plan needs the workflow-level command `/gsd-audit-milestone`, which cannot be run as a bash command inside an autonomous executor.

1. Run: `/gsd-audit-milestone v1.1`
   - If the command expects a bare version, run: `/gsd-audit-milestone 1.1`
   - If arguments are optional, running `/gsd-audit-milestone` is OK as long as it targets v1.1.

2. Confirm `.planning/v1.1-MILESTONE-AUDIT.md` is regenerated (frontmatter `audited:` timestamp changes).
   </instructions>
   <resume-signal>Type "done" once `.planning/v1.1-MILESTONE-AUDIT.md` is updated</resume-signal>
   </task>

<task type="auto">
  <name>Task 2: Record evidence that CI-01 and CI-04 are no longer flagged</name>
  <files>.planning/phases/15-ci-wiring-fixes/15-audit-milestone.md</files>
  <action>
Create `.planning/phases/15-ci-wiring-fixes/15-audit-milestone.md` capturing:

- The exact audit command run (`/gsd-audit-milestone ...`) and timestamp.
- A short excerpt of the "Integration Check" section showing CI wiring gaps.
- Explicit evidence that the old gap strings are gone from `.planning/v1.1-MILESTONE-AUDIT.md`:
  - "CI type-check step does not type-check (root tsc runs without a tsconfig)"
  - "Video smoke workflow invokes a non-existent CLI binary via pnpm exec"

Do not claim the entire milestone passes; this task is only about removing the CI-01/CI-04 unsatisfied wiring gaps.
</action>
<verify>
`test -f .planning/phases/15-ci-wiring-fixes/15-audit-milestone.md`

`node -e "const fs=require('fs'); const s=fs.readFileSync('.planning/v1.1-MILESTONE-AUDIT.md','utf8'); process.exit((s.includes('CI type-check step does not type-check')||s.includes('Video smoke workflow invokes a non-existent CLI binary'))?1:0)"`

`rg -n "CI type-check step does not type-check" .planning/v1.1-MILESTONE-AUDIT.md` # should have 0 matches

`rg -n "Video smoke workflow invokes a non-existent CLI binary" .planning/v1.1-MILESTONE-AUDIT.md` # should have 0 matches
</verify>
<done>Audit output no longer contains the CI-01/CI-04 gap strings, and an evidence note is recorded.</done>
</task>

</tasks>

<verification>
- `.planning/v1.1-MILESTONE-AUDIT.md` no longer contains the CI-01 or CI-04 unsatisfied wiring gap strings.
- `.planning/phases/15-ci-wiring-fixes/15-audit-milestone.md` exists and cites the audit rerun outcome.
</verification>

<success_criteria>

- Phase 15 ROADMAP success criterion #3 is satisfied: audit rerun does not flag CI-01 or CI-04 as unsatisfied.
  </success_criteria>

<output>
After completion, create `.planning/phases/15-ci-wiring-fixes/15-03-SUMMARY.md`
</output>
