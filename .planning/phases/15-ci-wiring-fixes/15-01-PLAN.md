---
phase: 15-ci-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/15-ci-wiring-fixes/15-01-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "CI type-check runs against real tsconfig projects (not a bare root tsc)"
    - "Type-check step fails CI on TypeScript errors"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow runs lint + real type-check + build + test"
      contains: "tsc --noEmit -p"
    - path: "packages/core/tsconfig.json"
      provides: "TypeScript project config for @tuireel/core"
      contains: "extends"
    - path: "packages/cli/tsconfig.json"
      provides: "TypeScript project config for tuireel CLI"
      contains: "extends"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "packages/core/tsconfig.json"
      via: "tsc -p"
      pattern: "tsc.*--noEmit.*-p packages/core/tsconfig\\.json"
    - from: ".github/workflows/ci.yml"
      to: "packages/cli/tsconfig.json"
      via: "tsc -p"
      pattern: "tsc.*--noEmit.*-p packages/cli/tsconfig\\.json"
---

<objective>
Fix CI so the "type-check" step actually type-checks the monorepo (core + cli) using explicit tsconfig projects.

Purpose: Satisfy CI-01 by making CI type-check meaningful (no false-green root tsc).
Output: Updated `.github/workflows/ci.yml` with explicit `tsc --noEmit -p ...` commands.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-workflow-polish/06-02-SUMMARY.md
@.github/workflows/ci.yml
@packages/core/tsconfig.json
@packages/cli/tsconfig.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make CI type-check target real tsconfig projects</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Update `.github/workflows/ci.yml` so the TypeScript type-check step runs against explicit projects, not a bare root `tsc`.

Implementation details:

- Replace `pnpm exec tsc --noEmit` with explicit project checks:
  - `pnpm exec tsc --noEmit -p packages/core/tsconfig.json`
  - `pnpm exec tsc --noEmit -p packages/cli/tsconfig.json`

Order constraint:

- Ensure the workflow still runs `pnpm build` before type-checking `packages/cli`, because the CLI TS check relies on `@tuireel/core`'s generated `dist/*.d.ts` (see `.planning/phases/06-workflow-polish/06-02-SUMMARY.md`).
- Keep overall job structure: install -> lint -> build -> type-check -> test.

Do not introduce a new root `tsconfig.json` or project references in this phase; keep changes minimal and CI-focused.
</action>
<verify>
`rg -n "tsc --noEmit -p packages/core/tsconfig\\.json" .github/workflows/ci.yml`

`rg -n "tsc --noEmit -p packages/cli/tsconfig\\.json" .github/workflows/ci.yml`

`rg -n "pnpm exec tsc --noEmit$" .github/workflows/ci.yml` # should have 0 matches

Local mirror of CI intent:

`pnpm build`

`pnpm exec tsc --noEmit -p packages/core/tsconfig.json`

`pnpm exec tsc --noEmit -p packages/cli/tsconfig.json`
</verify>
<done>CI workflow includes explicit `tsc --noEmit -p ...` commands and no longer runs a bare root `tsc`.</done>
</task>

</tasks>

<verification>
- `.github/workflows/ci.yml` runs explicit `tsc --noEmit -p packages/{core,cli}/tsconfig.json`.
- Local type-check commands pass in a clean working tree after `pnpm build`.
</verification>

<success_criteria>

- CI-01 is no longer false-green: type-check runs against a real TS project and fails on TS errors.
  </success_criteria>

<output>
After completion, create `.planning/phases/15-ci-wiring-fixes/15-01-SUMMARY.md`
</output>
