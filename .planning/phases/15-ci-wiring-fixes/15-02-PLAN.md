---
phase: 15-ci-wiring-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/video-smoke.yml
  - .planning/phases/15-ci-wiring-fixes/15-02-SUMMARY.md
autonomous: true

must_haves:
  truths:
    - "Video smoke CI can invoke tuireel CLI reliably (no command not found)"
    - "Video smoke CI still records MP4/WebM/GIF and validates outputs"
  artifacts:
    - path: ".github/workflows/video-smoke.yml"
      provides: "Video smoke CI job (record + validate + artifact upload on failure)"
      contains: "node packages/cli/dist/index.js"
    - path: "packages/cli/dist/index.js"
      provides: "Built CLI entry used by CI invocation"
      min_lines: 1
  key_links:
    - from: ".github/workflows/video-smoke.yml"
      to: "packages/cli/dist/index.js"
      via: "node invocation"
      pattern: "node packages/cli/dist/index\\.js record"
---

<objective>
Fix the video smoke workflow so it invokes the tuireel CLI deterministically from the repo (no `pnpm exec tuireel` at workspace root).

Purpose: Satisfy CI-04 by removing the "command not found" failure mode in CI.
Output: Updated `.github/workflows/video-smoke.yml` that runs the built CLI via `node packages/cli/dist/index.js ...`.
</objective>

<execution_context>
@/home/ubuntu/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.github/workflows/video-smoke.yml
@.planning/phases/06-workflow-polish/06-02-SUMMARY.md
@.planning/phases/11-ci-linting/11-02-SUMMARY.md
@packages/cli/package.json
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke CLI via built dist entry in video smoke workflow</name>
  <files>.github/workflows/video-smoke.yml</files>
  <action>
Update `.github/workflows/video-smoke.yml` to stop using `pnpm exec tuireel ...` (not available at workspace root).

Change the three record steps to run the built CLI entry directly:

- `node packages/cli/dist/index.js record packages/core/test/fixtures/smoke.tuireel.jsonc --format mp4`
- `node packages/cli/dist/index.js record packages/core/test/fixtures/smoke.tuireel.jsonc --format webm`
- `node packages/cli/dist/index.js record packages/core/test/fixtures/smoke.tuireel.jsonc --format gif`

Keep these invariants:

- Keep `pnpm build` before recording so `packages/cli/dist/index.js` exists.
- Keep ffprobe validation steps and the failure-only artifact upload unchanged, except for any necessary path tweaks if outputs change.

Do NOT add the `tuireel` workspace package as a root dependency in this phase; prefer the deterministic `node .../dist/index.js` invocation.
</action>
<verify>
`rg -n "pnpm exec tuireel" .github/workflows/video-smoke.yml` # should have 0 matches

`rg -n "node packages/cli/dist/index\\.js record" .github/workflows/video-smoke.yml`

Local mirror of CI intent:

`pnpm build`

`node packages/cli/dist/index.js --help`

`node packages/cli/dist/index.js record packages/core/test/fixtures/smoke.tuireel.jsonc --format mp4`

`test -f smoke-output.mp4`
</verify>
<done>Video smoke workflow invokes `node packages/cli/dist/index.js ...` for record steps and no longer relies on `pnpm exec tuireel` at repo root.</done>
</task>

</tasks>

<verification>
- Record steps in `.github/workflows/video-smoke.yml` run via `node packages/cli/dist/index.js`.
- A local run after `pnpm build` produces `smoke-output.mp4` from the fixture.
</verification>

<success_criteria>

- CI-04 video smoke job can invoke tuireel without "command not found".
  </success_criteria>

<output>
After completion, create `.planning/phases/15-ci-wiring-fixes/15-02-SUMMARY.md`
</output>
